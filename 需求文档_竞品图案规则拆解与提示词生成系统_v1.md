# 竞品图案规则拆解与提示词生成系统 — 需求文档 v1.1

> 文档日期：2026/06/30
> 上一版本：v1.0（2026/06/29）
> 状态：✅ 已确认（2026/06/30 阶段0验证通过）
> v1.1 更新：新增第 0 层"核心卖点锚定"、提示词改为三版输出（资料库版/AI推荐版/自定义模板版）
> 
> 阶段0验证结果：5张竞品图验证，4张通过（≥3张标准），验证通过。
> 进入阶段1开发。

---

## 一、项目定位

### 一句话说清楚这个系统干什么

设计师的**起手加速器**。把"看竞品 → 拆卖点 → 想方向 → 起几版设计"这前半段从大半天压到几分钟。

### 系统不是什么

- ❌ 不是一键出终稿的机器（终稿由设计师精修）
- ❌ 不是仿图洗图工具（保留卖点方向，具体表达全换）
- ❌ 不是灵感随机生成器（按规则生成，每一行提示词都有依据）

### 核心价值主张

> AI 按"规则"生成设计方案，不是按"灵感"生成。
> 规则 = **核心卖点锚定** + 商业动机 + 视觉结构 + 可变边界 + 产品适配 + 数据验证。

### 最高优先级原则（v1.1 新增）

> **提示词的构成必须围绕竞品图的核心卖点出发。**
> 核心卖点 = 买家下单的直接原因。它可能是一个视觉符号（骷髅手比心）、一个事件（250 周年）、一个视觉概念（名字连接根茎长出花朵）。
> **核心卖点绝对不能替换。** 所有改款操作必须围绕它展开、保留它、强化它——不能触碰它本身。

---

## 二、核心概念

### 2.1 规则卡（系统的核心资产）

一张爆款图拆出来的不是"一只恐龙的抠图"，而是一条**规则**。

**打比方**：
- 元素库 = 存了一堆食材（鸡蛋、番茄、葱花）
- 规则库 = 存了一堆**菜谱**（"番茄炒蛋：主料番茄+鸡蛋，火候大火快炒，适合家常"）

有了菜谱，食材可以换（番茄换西葫芦），但做法和"为什么好吃"的道理不变。

**规则卡的定义**：一张爆款图背后"为什么好卖"的结构化配方，包含 5 个层次（见 2.2）。
**文件格式**：JSON（每条规则一个文件，或存入数据库一条记录）。

### 2.2 规则卡的 6 层结构（v1.1 更新：从 5 层改为 6 层）

#### 第 0 层｜核心卖点锚定（最高优先级，v1.1 新增）

> 这一层定义"买家到底冲着什么下单的"。所有改款必须围绕它展开，绝不能替换它。

| 字段 | 说明 | 举例 |
|---|---|---|
| core_selling_point | 买家下单的直接原因 | "骷髅手比心形成爱心形状" |
| core_selling_point_type | 卖点类型 | visual_symbol（视觉符号）/ event（特定事件）/ visual_concept（视觉概念）/ emotional_hook（情感钩子） |
| why_it_sells | 一句话解释为什么它驱动下单 | "又酷又甜的暗黑浪漫反差——买家就是冲这个视觉来的" |
| lock_rule | 锁定规则 | "所有改款必须保留骷髅手比心，任何替换方案不得触碰这个符号" |

**判断方法**：问自己一个问题——"如果把这个东西换掉了，买家还会下单吗？"如果答案是"不会"或"大概率不会"，那它就是核心卖点。

**举例对比**：
| 竞品图 | 核心卖点 | 换掉后会怎样 |
|---|---|---|
| 小奶牛花卉女孩毯 | 可爱动物居中+花卉环绕+名字定制 | 换成恐龙依然成立（动物品种不是核心）✅ |
| 美国250周年拼块毯 | 美国250周年这个特定事件 | 换成"家庭传承"→买家不买了 ❌ |
| 骷髅手比心情侣毯 | 骷髅手比心这个视觉符号 | 换成猫咪比心→完全不同的产品 ❌ |
| Best Mom 照片拼贴 | Bootleg风+照片拼贴+3D金属字 | 换成MOM→DAD依然成立 ✅ |
| Mom's Garden 出生花 | 名字连接根茎长出花朵+简约花风 | 换成精致植物图鉴花→核心视觉概念丢了 ❌ |

每条规则卡包含 5 层，从"为什么卖"到"怎么验证"，层层递进：

#### 第 1 层｜商业层（卖给谁、为什么买）

> 决定设计方向，不是视觉问题，是生意问题。

| 字段 | 说明 | 举例 |
|---|---|---|
| target_audience | 目标人群 | 宠物主人 |
| use_scenario | 使用场景 | 宠物去世纪念、领养纪念 |
| purchase_motivation | 购买动机 | 表达对宠物的怀念，作为纪念礼物 |
| core_emotion | 核心情绪 | 怀念、陪伴、温柔、治愈 |
| price_sensitivity | 价格敏感度 | 中高（礼物型消费，愿意为情感买单） |

**作用**：如果 AI 不知道这一层，可能把宠物纪念图做成"搞笑萌宠"风格——情绪就偏了。商业层确保 AI 不会搞错设计方向。

#### 第 2 层｜视觉结构层（长什么样、怎么排）

> 定义画面的骨架：有哪些必备元素、各自在什么位置、什么风格、文字谁大谁小。

| 字段 | 说明 | 举例 |
|---|---|---|
| layout_formula | 构图公式 | 主体居中偏上 + 环绕装饰 + 下方文字区 |
| must_have_elements | 必备元素列表 | 宠物头像(居中偏上)、花环(环绕)、名字(下方)、日期(名字下方)、纪念短句(底部) |
| style | 视觉风格 | 柔和水彩 / 手绘线条 / 温暖插画 |
| color_mood | 色彩情绪 | 浅色背景、暖色调、柔和不刺眼 |
| text_hierarchy | 文字层级 | 名字最大 > 纪念短句 > 日期最小 |

**作用**：AI 生图时知道"骨架"是什么——不会把名字放角落、不会把花环放底下。

#### 第 3 层｜可变边界层（什么能换、什么不能换）

> 核心原则：爆款复用不是复制，而是"换元素、换主题、换人群、换节日"。

| 字段 | 说明 | 举例 |
|---|---|---|
| replaceable_elements | 可替换的元素/维度 | 狗→猫、花环→星星月亮、水彩→复古油画、英文→法语、宠物纪念→父亲节纪念 |
| must_not_change | 绝对不能改的 | 主体居中构图逻辑、名字+日期个性化结构、温柔治愈情绪基调、纪念礼物商业定位 |

**作用**：AI 知道换什么是安全的、换什么会破坏爆款 DNA。规则明确告诉它可变的边界在哪里。

**替换维度支持以下几种操作**：
- 保留构图，只换主题（宠物→父亲节）
- 保留风格，只换元素（花环→星星月亮）
- 保留情绪，只换产品（相框→毛毯）
- 保留文字结构，只换节日（圣诞→母亲节）
- 保留销售人群，只换视觉手法（水彩→复古油画）

#### 第 4 层｜产品适配层（同一条规则放到不同产品上怎么改）

> 同一条规则，放到不同产品上，画面需要调整。

| 产品类型 | 适配要求 | 举例 |
|---|---|---|
| 相框 | 精致感，花环细，文字清晰，整体像纪念礼物 | 画布 1:1 或 4:5，去掉复杂背景 |
| 毛毯 | 面积大，画面丰富，主体放大，可加边框/星星/云朵 | 画布 3:4 或 2:3，增加装饰密度 |
| 衣服 | 简洁，主体大，文字少，花环不能复杂，远看有识别度 | 根据印花区域，删小日期 |
| 沙滩巾 | 大胆，颜色亮，主体大，满版或大图案，小字不要 | 画布 1:2，远看有识别度 |

**作用**：一条规则可以生成 N 种产品的提示词，每种自动适配产品特性。不是万能模板硬套。

#### 第 5 层｜数据验证层（这条规则值不值得复用）

> 用数据回答"这条规则靠不靠谱"。

| 字段 | 说明 | 举例 |
|---|---|---|
| source_sales_rank | 来源竞品的销量排名 | BSR 前 500 |
| proven_platforms | 已验证平台 | Amazon, Etsy |
| seasonal_dependency | 季节依赖度 | 低（全年可卖） / 高（仅圣诞） |
| ip_dependency | IP 依赖度 | 无 / 有（依赖某动画形象） |
| reuse_level | 复用等级（基于以上数据综合判断） | S / A / B / C |

### 2.3 复用等级（SABC 分级）

不是所有爆款都值得拆成规则。用 SABC 分级减少工作量：

| 等级 | 标准 | 处理方式 | 举例 | 预估占比 |
|---|---|---|---|---|
| **S 级** | 跨产品、跨主题、跨节日通用 | 完整拆 5 层规则卡，重点维护 | 名字定制、宠物纪念、家庭纪念 | ~10-15% |
| **A 级** | 相近产品可复用 | 拆 3 层（商业+视觉+可变边界），产品适配按需补 | 相框→毛毯，T恤→卫衣 | ~20-25% |
| **B 级** | 小范围改款 | 只记关键特征标签，不做完整规则卡 | 强依赖某节日、某文案梗 | ~30% |
| **C 级** | 不建议复用 | 跳过不拆，存标签备查 | 依赖 IP、热点、特定图片 | ~30-35% |

**实际效果**：480 张图 → 认真拆 ~70 张 S 级 + 粗拆 ~100 张 A 级 + 其余简单标记或跳过。

---

## 三、系统流程

系统分两个阶段使用：先一次性建库，再日常使用。

### 3.1 一次性建库阶段

```
你的 480 张自有图 + 你关注的竞品图
              ↓
   ┌─────────────────────────────┐
   │  AI 初步分析每张图，给出：   │
   │  · 主题/风格/情绪/元素      │
   │  · 建议的 SABC 等级         │
   └─────────────────────────────┘
              ↓
      人工确认 SABC 等级
      （可能 AI 判 S 级你觉得只值 B 级，手动改）
              ↓
   ┌─────────────────────────────┐
   │  S/A 级图：                  │
   │  AI 按 5 层结构拆解规则      │
   │  → 输出规则卡 JSON 草稿     │
   └─────────────────────────────┘
              ↓
      人工校对规则卡
      （看看 AI 拆得对不对，商业层情绪对不对，
       可变边界合不合理，产品适配准不准）
              ↓
      确认 → 存入规则库
              ↓
      规则库建成（约 70-150 条规则卡）
```

### 3.2 日常使用阶段（核心流程）

```
你上传一张新竞品图
              ↓
  ┌───────────────────────────────┐
  │ 第 1 步：AI 分析竞品图         │
  │ · 拆出 5 层结构               │
  │ · 商业层（卖给谁/为什么买）    │
  │ · 视觉结构（有什么/怎么排）    │
  │ · 可变边界（能换什么/不能换什么）│
  │ · 产品适配（各产品怎么调）     │
  │ · 数据验证（值不值得复用）     │
  └───────────────────────────────┘
              ↓
  ┌───────────────────────────────┐
  │ 第 2 步：匹配规则库            │
  │                               │
  │ 库里有类似规则吗？             │
  │ ├─ 有 → 复用已有规则          │
  │ │   （提示用户：这和你库里的   │
  │ │    "宠物纪念花环规则"相似，  │
  │ │    建议基于该规则改款）       │
  │ │                             │
  │ └─ 没有 → 基于分析新建规则卡   │
  │     （人工校对后存入库，       │
  │      规则库越用越大）          │
  └───────────────────────────────┘
              ↓
  ┌───────────────────────────────┐
  │ 第 3 步：你选择改款方式        │
  │                               │
  │ · 要做什么产品？（毛毯/衣服/…）│
  │ · 想换哪些元素？              │
  │   （狗→猫？花环→星星？）      │
  │ · 想换什么风格？              │
  │   （水彩→复古？卡通→极简？）  │
  │ · 想换什么场景/节日？          │
  │   （宠物纪念→父亲节？）       │
  │                               │
  │ （这一步可以人工选择，         │
  │  也可以让 AI 推荐几个方向）    │
  └───────────────────────────────┘
              ↓
  ┌───────────────────────────────┐
  │ 第 4 步：生成结构化提示词      │
  │                               │
  │ 基于：                        │
  │ · 规则卡的 5 层内容           │
  │ · 你选择的替换元素/产品/风格   │
  │ · 第 4 层产品适配规则          │
  │                               │
  │ 输出：                        │
  │ · 完整提示词（正向+禁止事项）  │
  │ · 改款说明（保留了什么/改了什么）│
  │ · 资料库匹配结果              │
  │   （用了哪条规则/哪些元素参考） │
  └───────────────────────────────┘
              ↓
  ┌───────────────────────────────┐
  │ 第 5 步：人工调整提示词        │
  │                               │
  │ · 你可以改任意一行            │
  │ · 改完确认 → 最终版提示词      │
  └───────────────────────────────┘
              ↓
        最终交付物：
        · 规则卡（JSON）
        · 结构化提示词（可调整版）
        · 改款说明

  ┌───────────────────────────────┐
  │ 后续（等测试 OK 后接入）：     │
  │ · 把提示词丢给生图 AI 出图    │
  │ · 设计师精修终稿              │
  └───────────────────────────────┘
```

---

## 四、规则卡完整数据结构

以下是一张规则卡的完整 JSON 结构定义。每个字段都有说明。

```json
{
  "rule_id": "RULE-0042",
  "rule_name": "宠物纪念花环规则",
  "reuse_level": "S",
  "source_images": ["竞品图_038.jpg", "自有图_112.jpg"],
  "created_date": "2026-06-29",
  "last_updated": "2026-06-29",

  "layer_1_commercial": {
    "target_audience": ["宠物主人"],
    "use_scenario": ["宠物去世纪念", "宠物生日", "领养纪念"],
    "purchase_motivation": "表达对宠物的怀念和爱，作为纪念礼物",
    "core_emotion": ["怀念", "陪伴", "温柔", "治愈"],
    "price_sensitivity": "中高（礼物型消费，愿意为情感买单）"
  },

  "layer_2_visual_structure": {
    "layout_formula": "主体居中偏上 + 环绕装饰 + 下方文字区",
    "must_have_elements": [
      {
        "slot": "主视觉",
        "description": "宠物头像或全身像",
        "position": "居中偏上",
        "visual_weight": "最重，第一眼看到的"
      },
      {
        "slot": "环绕装饰",
        "description": "花环 / 星星 / 云朵 / 藤蔓",
        "position": "围绕主视觉",
        "visual_weight": "中等，辅助但不抢主体"
      },
      {
        "slot": "名字",
        "description": "宠物名字",
        "position": "下方居中",
        "visual_weight": "文字中最大"
      },
      {
        "slot": "日期",
        "description": "出生日期或纪念日期",
        "position": "名字下方",
        "visual_weight": "文字中较小"
      },
      {
        "slot": "纪念短句",
        "description": "如 Forever in my heart",
        "position": "底部或顶部",
        "visual_weight": "文字中最小"
      }
    ],
    "style": "柔和水彩 / 手绘线条 / 温暖插画",
    "color_mood": "浅色背景、暖色调、柔和不刺眼",
    "text_hierarchy": "名字最大 > 纪念短句 > 日期最小"
  },

  "layer_3_variable_boundaries": {
    "replaceable_elements": {
      "宠物种类": {
        "original": "狗",
        "alternatives": ["猫", "兔子", "仓鼠", "鸟"]
      },
      "环绕装饰": {
        "original": "花环",
        "alternatives": ["星星月亮", "云朵", "藤蔓蝴蝶", "爪印"]
      },
      "风格变体": {
        "original": "水彩",
        "alternatives": ["复古油画", "简约线描", "3D卡通", "素描"]
      },
      "文案语言": {
        "original": "英文",
        "alternatives": ["德语", "法语", "西班牙语"]
      },
      "场景切换": {
        "original": "宠物纪念",
        "alternatives": ["父亲节纪念", "家庭纪念", "毕业纪念"]
      }
    },
    "must_not_change": [
      "主体居中的构图逻辑",
      "名字+日期的个性化结构",
      "温柔治愈的情绪基调",
      "纪念礼物的商业定位"
    ]
  },

  "layer_4_product_adaptation": {
    "相框": {
      "canvas_ratio": "1:1 或 4:5",
      "adaptation_notes": "保留精致感，花环细一点，文字清晰，整体像纪念礼物",
      "simplify": ["去掉复杂背景纹理"],
      "enhance": []
    },
    "毛毯": {
      "canvas_ratio": "3:4 或 2:3",
      "adaptation_notes": "画面可以更丰富，宠物放大，可加边框/星星/云朵/背景纹理",
      "simplify": [],
      "enhance": ["面积大，可增加装饰密度", "可加背景纹理"]
    },
    "衣服": {
      "canvas_ratio": "根据印花区域",
      "adaptation_notes": "更简洁，宠物头像要大，文字要少，花环不能太复杂，远看有识别度",
      "simplify": ["删掉小日期", "花环简化"],
      "enhance": []
    },
    "沙滩巾": {
      "canvas_ratio": "1:2",
      "adaptation_notes": "更大胆，颜色更亮，主体更大，满版或大图案",
      "simplify": ["小字基本不要"],
      "enhance": ["颜色加饱和", "主体放大", "适合满版"]
    }
  },

  "layer_5_data_validation": {
    "source_sales_rank": "竞品在亚马逊 BSR 前 500",
    "proven_platforms": ["Amazon", "Etsy"],
    "seasonal_dependency": "低（全年可卖）",
    "ip_dependency": "无",
    "reuse_level_reason": "S 级：跨产品（相框/毛毯/衣服/沙滩巾）、跨场景（宠物纪念/父亲节/家庭纪念）、无 IP 依赖、全年可卖"
  },

  "prompt_template": {
    "structure": "基于【{rule_name}】重新设计一款【{product_type}】图案。",
    "fields": [
      "目标人群：{target_audience}",
      "使用场景：{use_scenario}",
      "核心情绪：{core_emotion}",
      "必须保留：{must_have_elements 的描述}",
      "可替换元素：{用户选择的替换方案}",
      "构图要求：{layout_formula}",
      "风格要求：{style}，{color_mood}",
      "文字结构：{text_hierarchy}",
      "产品适配要求：{对应产品的 adaptation_notes}",
      "禁止事项：{must_not_change 的反面 + 通用禁止项}",
      "输出要求：无文字版（文字后期叠加）"
    ]
  }
}
```

---

## 五、系统模块设计

### 模块 1：SABC 分级模块

**功能**：对输入图片做初步分析，判断其复用价值等级。

**输入**：一张图片（竞品图或自有图）。

**处理逻辑**：
1. AI（VLM）识别图片的主题、风格、元素、文字。
2. 判断以下指标：
   - 是否依赖特定 IP / 明星 / 热点事件 → 降级
   - 是否强绑定单一节日且无法迁移 → 降级
   - 是否包含可个性化结构（名字/日期/照片位） → 升级
   - 视觉规则是否可迁移到其他产品 → 升级
3. 输出建议等级（S/A/B/C）+ 判断理由。
4. **人工确认**后生效。

**输出**：
```json
{
  "image": "竞品图_038.jpg",
  "suggested_level": "S",
  "reason": "宠物纪念主题，包含名字+日期个性化结构，不依赖 IP，全年可卖，构图可迁移到多种产品",
  "confirmed_level": null  // 等人工确认
}
```

### 模块 2：规则拆解模块

**功能**：对 S/A 级图片，按 5 层结构拆解出规则卡。

**输入**：一张已确认为 S 或 A 级的图片。

**处理逻辑**：
1. AI（VLM）分析图片，按 5 层结构逐层输出。
2. 每层使用**受控词表**（AI 从预定义选项里选，不自由发挥）：
   - 目标人群词表：妈妈、爸爸、情侣、宠物主人、老师、孩子、朋友……
   - 使用场景词表：生日、纪念日、圣诞、母亲节、宠物纪念、毕业……
   - 核心情绪词表：温柔、治愈、活泼、童趣、搞笑、复古、酷……
   - 风格词表：卡通插画、水彩、写实、复古油画、极简线描、3D 卡通……
   - 色彩情绪词表：鲜艳饱和、柔和暖色、冷色调、黑白、复古暖黄……
3. 输出规则卡 JSON 草稿。
4. **人工校对**后入库。

**输出**：一张完整的规则卡 JSON（结构见第四章）。

**A 级图的简化处理**：只拆第 1-3 层（商业 + 视觉结构 + 可变边界），第 4-5 层按需补充。

### 模块 3：规则匹配模块

**功能**：上传新竞品图时，从规则库中找是否有类似的已有规则。

**输入**：新竞品图的 AI 分析结果（5 层结构）。

**处理逻辑**：
1. 提取新图的关键特征：主题、构图公式、情绪、元素组合。
2. 与规则库中已有规则卡对比，按以下维度计算匹配度：
   - 主题匹配（宠物纪念 vs 宠物纪念 → 高匹配）
   - 构图公式匹配（主体居中+环绕 vs 主体居中+环绕 → 高匹配）
   - 情绪匹配（治愈 vs 治愈 → 高匹配）
   - 元素组合匹配（花环+名字+日期 vs 花环+名字+日期 → 高匹配）
3. 输出：
   - 如果匹配度高：推荐复用已有规则，标注差异点
   - 如果匹配度低：建议新建规则卡

**输出**：
```json
{
  "match_found": true,
  "matched_rule": "RULE-0042",
  "matched_rule_name": "宠物纪念花环规则",
  "match_score": 0.85,
  "differences": ["竞品用的是猫不是狗", "装饰是星星不是花环"],
  "recommendation": "建议基于'宠物纪念花环规则'改款，将狗替换为猫、花环替换为星星"
}
```

### 模块 4：提示词生成模块（v1.1 重大更新：三版输出）

**功能**：基于规则卡 + 核心卖点锚定 + 用户选择，生成**三个版本**的提示词。

**核心规则**：所有版本的提示词必须围绕第 0 层核心卖点构建。核心卖点在提示词中用 🔒 标记，不可修改。

#### 版本 A｜资料库关联版

结合自有资料库中已有的类似元素/风格参考，围绕核心卖点生成提示词。

**生成逻辑**：
1. 锁定核心卖点（第 0 层），原封不动写入提示词。
2. 在资料库中搜索与当前规则匹配的自有图/元素。
3. 如果找到 → 把自有元素的风格特征写入提示词（如"参考资料库 #elem_067 的暗黑小玫瑰风格"）。
4. 如果没找到 → 标注"资料库中暂无匹配，以下为基于规则的默认方案"。

**适用场景**：用户库里有类似风格/元素，想让新图和已有产品线风格统一。

#### 版本 B｜AI 推荐风格版

AI 围绕核心卖点，推荐一个改款方向。核心卖点不动，只在装饰/配色/风格细节上做变化。

**生成逻辑**：
1. 锁定核心卖点，不动。
2. 从第 3 层可变边界中选择替换组合。
3. 附上推荐理由（为什么这么改、改了什么、保留了什么）。

**适用场景**：用户没有特定想法，让 AI 建议一个方向。

#### 版本 C｜自定义模板版（下拉框选项）

给用户一个结构化模板，核心卖点已锁定（灰色不可编辑），每个可变项做成下拉框选项，用户自己选。

**生成逻辑**：
1. 核心卖点自动锁定，显示为灰色不可编辑区域。
2. 第 3 层可变边界的每个可替换维度 → 生成一个下拉框。
3. 每个下拉框包含：原版选项（默认）+ 3-5 个替代选项。
4. 用户选完后系统自动组装成完整提示词。

**适用场景**：用户有自己的想法，想手动控制每个替换项。

**交互规则**：
- 如果用户选择了版本 A 或版本 B → 直接使用，版本 C 折叠不显示。
- 只有用户明确点击"我要自定义"时才展开版本 C。
- 无论哪个版本，最终提示词都可人工二次编辑（但核心卖点区域会有警告提示："修改此项可能导致丢失核心卖点"）。

**每个版本都同时输出**：
- 中文结构化提示词（人可读，含每行的规则依据）
- 英文生图提示词（正向 + 负向，可直接复制到生图工具）
- 改款说明（保留了什么 / 改了什么 / 基于哪条规则）

### 模块 5：人工调整界面

**功能**：用户查看并手动调整提示词的每一行。

**MVP 阶段极简**：
- 展示规则卡内容（可折叠查看 5 层）
- 展示生成的提示词（可编辑每一行）
- "换一个方向"按钮（重新选择替换元素/风格/产品）
- "确认/下载"按钮

**不做**：复杂审核工作流、拖拽编辑器、多人协作。

---

## 六、安全防线

五道防线确保"保留卖点、换掉表达"：

| 防线 | 做法 |
|---|---|
| 1. 不用竞品原图像素 | 绝不抠竞品图上的恐龙/狗/文字直接用 |
| 2. 不让生图模型强参考竞品图 | 竞品图只在分析阶段被"读"，不进入生图环节 |
| 3. 只提取高层卖点 | 提取的是"规则"（多只彩色卡通恐龙+童趣+名字牌），不是"画面" |
| 4. 具体表达全换 | 规则卡的第 3 层明确列出哪些必须换掉 |
| 5. 生成后人工看一遍 | 太像竞品就继续调整提示词 |

---

## 七、分阶段实施计划

### 阶段 0：人工验证（先确认路能走通）

**目标**：不写代码，手动完成 5-10 个完整的"竞品图 → 规则卡 → 提示词 → AI 生图"案例，验证这条路走不走得通。

**做法**：
1. 选 5-10 张有代表性的竞品图（不同主题/风格/产品）。
2. 人工（或用 AI 辅助）按 5 层结构拆出规则卡。
3. 人工按规则卡写提示词。
4. 把提示词丢到生图 AI（即梦/MJ/SD）里出图。
5. 判断：出来的图是否达到"保留卖点、换掉表达"的标准？

**验收标准**：
- 至少 3 张竞品图能走通完整流程
- 生成的 B 图一眼看出是同品类同卖点，但具体画面明显不同
- 规则卡的 5 层结构确实对提示词有指导作用（不是写了没用的花架子）

**交付物**：
- 5-10 张规则卡样本（手工填的 JSON）
- 对应的提示词
- 对应的 AI 生图结果
- 验证结论（哪些 OK、哪些有问题、需要调整什么）

### 阶段 1：规则拆解系统

**目标**：让 AI 能半自动拆解规则卡，人工校对后入库。

**做法**：
1. 实现 SABC 分级模块。
2. 实现 5 层规则拆解模块（含受控词表）。
3. 对 480 张图跑分级 + 对 S/A 级图跑规则拆解。
4. 人工校对入库。

**验收标准**：
- AI 分级准确率 ≥ 70%（人工修正后 100%）
- S 级规则卡 AI 拆解准确率 ≥ 60%（人工校对补完后可用）
- 建成规则库（约 70-150 条可用规则卡）

**交付物**：
- 分级模块
- 规则拆解模块
- 规则库（JSON 文件集合）

### 阶段 2：提示词生成系统

**目标**：上传竞品图 → 匹配规则 → 生成结构化提示词。

**做法**：
1. 实现竞品图分析（对新图做 5 层拆解）。
2. 实现规则匹配模块（在规则库中找类似规则）。
3. 实现提示词生成模块（规则卡 + 替换选择 + 产品适配 → 提示词）。
4. 实现人工调整界面（极简版）。

**验收标准**：
- 上传竞品图后 1 分钟内输出分析 + 提示词
- 提示词每一行有规则依据（不是灵感随机）
- 人工可编辑每一行
- 匹配到已有规则时能正确推荐

**交付物**：
- 完整的日常使用流程
- 极简操作界面

### 阶段 3：自有图库参考池

**目标**：从 480 张整图中按主题/风格/色彩找相似参考，辅助提示词生成。

**做法**：
1. 对 480 张图打标签（主题、风格、色彩情绪，复用规则拆解时已有的信息）。
2. 生成提示词时，从库中推荐 1-3 张风格/主题相近的自有图作为参考。
3. 在提示词中标注"参考自有图 xxx"。

**验收标准**：
- 推荐的参考图与目标规则的风格/主题相关
- 提示词中有标注参考来源

**交付物**：
- 自有图库标签系统
- 参考图推荐功能

### 阶段 4（后续）：精细元素库 + 接入生图 AI

- 元素级切割、标签、向量检索
- 接入生图 AI 自动出图
- 批量处理
- 其他商用化功能

---

## 八、验收标准汇总

### 整体验收

| 指标 | 标准 |
|---|---|
| 规则卡质量 | 5 层结构完整，每层字段有内容，商业层情绪准确，可变边界合理 |
| 提示词质量 | 每一行有规则依据，不是灵感随机；换产品时提示词自动适配 |
| 规则匹配 | 类似竞品能匹配到已有规则，差异点能被识别 |
| 改款方向 | 一眼看出是同品类同卖点，但具体画面明显不同 |
| 人工可控 | 每一步人工可介入校对/调整 |
| 效率提升 | 从"设计师看竞品半天起稿"到"几分钟出提示词" |

---

## 九、技术选型（简要）

| 能力 | 推荐方案 |
|---|---|
| 图片分析（看懂竞品） | VLM（GPT-4o / Claude Vision / Qwen-VL），受控词表约束输出 |
| 文字识别（读图上的字） | OCR（PaddleOCR / 系统内置 OCR） |
| 规则匹配 | 前期：关键词 + 标签匹配；后期：可加向量相似度 |
| 规则/提示词存储 | JSON 文件 → 后期可迁移到数据库 |
| 人工界面 | 极简 Web 页面（后端 Python/Node + 前端简单表单） |
| 生图（阶段 4 接入） | 待定（即梦/MJ/SD/Flux，根据阶段 0 验证结果选型） |

---

## 十、术语表

| 术语 | 含义 |
|---|---|
| 规则卡 | 一张爆款图背后"为什么好卖"的结构化配方，包含 5 层 |
| 规则库 | 所有规则卡的集合 |
| SABC 分级 | 判断一条规则的复用价值等级 |
| 5 层结构 | 商业层 → 视觉结构层 → 可变边界层 → 产品适配层 → 数据验证层 |
| 槽位（slot） | 画面中的一个元素位置（如"主视觉位"、"环绕装饰位"） |
| 受控词表 | AI 只能从预定义选项中选择，不能自由发挥的字段 |
| 可变边界 | 一条规则中哪些能替换、哪些不能替换的明确界定 |
| 产品适配 | 同一条规则放到不同产品上（相框/毛毯/衣服/沙滩巾）的调整规则 |

---

> **下一步**：请确认本需求文档。确认后进入阶段 0（人工验证），我会准备规则卡模板和验证流程。
