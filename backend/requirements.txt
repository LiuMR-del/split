fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
httpx>=0.27
pydantic>=2.0
Pillow>=10.0
pillow-heif>=0.18
# 元素拆分透明底的连通性判定（services/image_alpha_utils.py）。
# 缺失时代码不会报错，但会静默降级为纯阈值抠图——浅色主体会重新出现破洞，必须安装
scipy>=1.10
