# 需求三期实施方案：运营反馈四项功能

- **创建日期**: 2026-08-17
- **状态**: 已定稿，待实施
- **需求来源**: 运营反馈 4 条，方案经用户确认（决策记录见 §1.2）
- **实施者须知**: 本文档是三期的**现行方案文档**。实施中的任何方案/规则变更，当场记入本文档（带日期和理由）。每个阶段收尾时做文档对账：CLAUDE.md（宪法）、本文档（方案）、§9 工单（只追加不回改）三层冲突清零才算收尾。**每完成一个阶段必须停下来等用户测试验收确认，禁止跳过确认直接推进下一阶段。**

---

## 1. 背景与决策记录

### 1.1 运营原始反馈（4 条）

1. 在自定义板块（版本C）加一个衍生版本：将图案的每种元素列成清单，分类生成"蒙版图"并下载，支持勾选下载。
2. 自定义添加的目标产品持久化，后续可复用；尺寸也要支持保存持久化。
3. 版本B 加衍生：AI 基于规则卡多搭配几套设计方案，在选择数量处让使用者选择生成几张图（几套方案各出一张）。
4. 批量上传图案，逐张分析给出规则卡。

### 1.2 已确认决策（2026-08-17，用户拍板）

| # | 决策点 | 结论 |
|---|--------|------|
| 1 | 需求1 "蒙版图"的定义 | 按**理解 A：元素分离素材图**实现（每个元素单独一张干净背景的图），不做黑白二值蒙版。开工前先做 Spike 验证抠取质量，Spike 结论决定 go/no-go |
| 2 | 需求2 持久化位置 | **落后端**（`data/user_prefs.json`），不用 localStorage——换浏览器/清缓存不丢 |
| 3 | 需求3 交互形态 | 方案卡片切换 + 勾选批量生成，上限 4 套。**生成完之后必须保持现有的展现形式**（每套方案仍通过现有 PromptDisplay 渲染，版本B 面板整体布局不变），因为使用者可能同时还在用版本C（自定义版本），不能破坏三栏并排布局 |
| 4 | 需求4 批量保存策略 | 分析成功**自动保存**进规则库；**每张分析完成后其结果条目收起（紧凑行）**，队列继续跑下一张 |
| 5 | 实施顺序 | **阶段一=需求2 → 阶段二=需求4 → 阶段三=需求3 → 阶段四=需求1**（先小后大、风险后置） |
| 6 | 需求1 Spike 后续（2026-08-17 追加） | Spike 首轮结论：干净✅ / 透明底❌ / 保真部分风格偏移（见 §6.7）。**先做一轮指令调优再定功能定位**（风格锁定 ± mockup 消除两变体 × 海龟/珊瑚，≈4 次调用）；**无论调优结局均为 go**，定位文案按结果三档落地（详见 §6.7 用户决策块）。透明底选项永久禁止、前端不得假设方图 |
| 6b | 需求1 验收后修正（2026-08-17 追加，**推翻 6 的透明底结论**） | 用户验收反馈四项，调查后：位置/比例可修（第一版模板 `centered` 写反了方向）；元素口径收窄为**只要图形元素**；**透明底可以做**——首轮 Spike 漏了 OpenAI images API 的 `background="transparent"` **参数**（只在 prompt 文字里要求过），加参数实测真 RGBA。但该参数与"保持原位置"不可兼得，故定为"**位置优先 + 后端 `white_to_transparent()` 转透明**"。批量拆分保持串行。详见 §6.9 变更记录（二） |
| 7 | 需求1 入口交互（2026-08-17 追加） | 元素拆分是低频功能（只有个别产品用得到）：**默认零加载零请求，只渲染一个入口按钮，点击后才加载元素清单并展开面板**；生成仍需勾选元素后二次点击，入口按钮绝不自动开始生成（详见 §6.4.1 变更块） |

---

## 2. 全局约束（实施前必读）

以下是项目 CLAUDE.md 中与本期直接相关的铁律，逐条列出防止踩坑。**冲突时以 CLAUDE.md 为准**。

1. **Python 3.9 兼容**：必须用 `Optional[X]` / `List[X]`（从 typing 导入），禁 `X | None` / `list[X]`。
2. **Import 风格**：后端绝对路径导入（`from services.xxx import yyy`），不用相对导入。
3. **新增 API 响应格式**：统一用 `{"success": True, "data": ...}` 包装（CLAUDE.md 已知待改进项明确要求新路由用包装格式）。前端用 `lib/api.ts` 的 `unwrapData<T>()` 解包。
4. **JSON 落盘**：所有用户数据 JSON 写入用 `services/file_utils.py` 的 `atomic_write_json(path, data)`（防进程中断截断）。
5. **单进程部署**：并发保护只用进程内 `threading.Lock`，禁 `--workers >1` 假设。
6. **给 AI 看的 prompt 里 JSON 输出示例不能给具体值**：可变字段一律用方括号占位符描述"填什么、怎么判断"（历史教训：示例具体值会被模式匹配抄写）。
7. **AI 温度约定**（#13）：结构化任务传 `temperature=0`，创意任务（改款推荐）**不传**保持多样性。
8. **新路由异常处理**：except 块用 `logging.exception` 打印完整异常栈，HTTPException detail 给 `str(e)`。
9. **前端**：全部 Client Component（`'use client'`）；Tailwind v4 主题在 `globals.css` `@theme inline`，颜色用 `codex-*`；写 Next.js 代码前先读 `frontend/node_modules/next/dist/docs/`（Next.js 16 有 breaking changes）。
10. **编辑指令式提示词的语言策略**（批次四）：`_edit_lang_value` 优先取 `*_en`，取不到时**放行原始中文值**（中文只是给模型定位图里的元素，模型同时能看到图）。阶段四的元素抠取指令沿用此策略。
11. **每阶段收尾**：`cd frontend && npm run lint` 通过；手动回归验收清单全过；CLAUDE.md 增补该阶段的关键架构约定；本文档对账。

**验证环境**：一键启动 `启动项目.command`（后端 8000 + 前端 3000）；后端带 `--reload`。AI 分析和生图功能验收需要 `backend/data/config.json`（AI 模型）与 `backend/data/gen_config.json`（生图）已配置——实施者改完代码后，涉及 AI/生图的验收项由用户在真实配置下操作确认。

---

## 3. 阶段一：目标产品 + 尺寸的后端持久化（需求2）

### 3.1 目标与现状

**目标**：用户在 A/B/C 任一版本用"✏️ 自定义"输入过的目标产品名、以及手动输入的生图宽高，持久化到后端；下次打开任何规则卡都能直接点选；支持删除；生图尺寸额外记住"上次使用的尺寸"并自动恢复。

**现状（代码事实）**：
- `frontend/components/prompts/ProductSelect.tsx`：A/B/C 三版共用的目标产品下拉组件，自定义输入只存在组件受控 value 里，刷新即丢。
- `frontend/components/prompts/PromptDisplay.tsx` 的 `ImageGenSection`（约 548 行起）：`PRODUCT_SIZE_PRESETS`（17 个硬编码预设，391 行起）+ `sizePreset/width/height` state，不持久，每次进页面回到 `square 1024×1024`。
- 项目已有 `lib/localStorage.ts`（版本C 自定义值），**本阶段不动它**——版本C 自定义"维度值"按规则卡隔离是合理的，与本阶段的"全局"产品/尺寸是两种性质的数据。

### 3.2 后端设计

#### 3.2.1 新文件 `backend/services/user_prefs_store.py`

```
数据文件: backend/data/user_prefs.json
结构:
{
  "custom_products": ["Frame 相框", "Pillow 抱枕"],          // string[]，最新在前
  "custom_size_presets": [                                    // 最新在前
    {"label": "抱枕 45x45", "width": 3000, "height": 3000}
  ],
  "last_size": {"preset": "square", "width": 1024, "height": 1024}   // 或 null
}
```

模块内容（签名级设计，Python 3.9 typing）：

- 模块级 `_LOCK = threading.Lock()`；`PREFS_PATH = Path(__file__).parent.parent / "data" / "user_prefs.json"`。
- `DEFAULT_PREFS: dict = {"custom_products": [], "custom_size_presets": [], "last_size": None}`。
- `load_prefs() -> dict`：读文件；文件不存在/JSON 损坏/非 dict → 返回 `DEFAULT_PREFS` 的深拷贝。**shape 校验**（参考 `lib/localStorage.ts` 的防御精神）：
  - `custom_products`：只保留非空 str，去重保序；
  - `custom_size_presets`：只保留形如 `{label: 非空str, width: int 1~10000, height: int 1~10000}` 的项（width/height 收到 float 时取 int）；
  - `last_size`：不满足 `{preset: str, width: int, height: int}` 则置 None。
  - 校验只过滤不抛错，损坏字段静默丢弃。
- `add_custom_product(name: str) -> List[str]`：`with _LOCK:` load → name 做 `strip()`，空/长度>100 抛 `ValueError`（路由转 400）→ 去重（精确匹配，已存在则先移除再插头部，实现"最新在前"）→ 截断保留 20 条 → `atomic_write_json` → 返回更新后的列表。
- `remove_custom_product(name: str) -> List[str]`：锁内 load → 过滤掉精确匹配项 → 原子写 → 返回列表（不存在也不报错，幂等）。
- `add_custom_size(label: str, width: int, height: int) -> List[dict]`：锁内；label strip 后空/长度>50 抛 ValueError；width/height 不在 1~10000 抛 ValueError；**按 label 去重**（同名视为更新，移除旧项插头部）→ 截断 20 条 → 原子写 → 返回列表。
- `remove_custom_size(label: str) -> List[dict]`：同上幂等删除。
- `set_last_size(preset: str, width: int, height: int) -> dict`：锁内校验 width/height 1~10000（preset 允许空串=手动模式）→ 写入 → 返回 last_size。

注意：本 store 无递增 ID，不需要占位文件机制；锁 + 原子写足够。

#### 3.2.2 新文件 `backend/routers/user_prefs.py`

`router = APIRouter(prefix="/prefs", tags=["用户偏好"])`，在 `main.py` 注册：`app.include_router(user_prefs_router, prefix="/api")`。

| 方法+路径 | 请求体（pydantic v2 模型） | 响应（统一包装格式） |
|---|---|---|
| `GET /api/prefs` | — | `{"success": True, "data": {custom_products, custom_size_presets, last_size}}` |
| `POST /api/prefs/custom-products` | `{"name": str}` | `{"success": True, "data": {"custom_products": [...]}}` |
| `DELETE /api/prefs/custom-products?name=...` | query 参数（值可能含空格/中文，前端 `encodeURIComponent`） | 同上 |
| `POST /api/prefs/custom-sizes` | `{"label": str, "width": int, "height": int}` | `{"success": True, "data": {"custom_size_presets": [...]}}` |
| `DELETE /api/prefs/custom-sizes?label=...` | query 参数 | 同上 |
| `PUT /api/prefs/last-size` | `{"preset": str, "width": int, "height": int}` | `{"success": True, "data": {"last_size": {...}}}` |

- store 抛 `ValueError` → 路由转 `HTTPException(400, detail=str(e))`。
- 其余未预期异常 → `logging.exception` + 500。

### 3.3 前端设计

#### 3.3.1 抽取共享尺寸预设：新文件 `frontend/lib/sizePresets.ts`

把 `PromptDisplay.tsx` 里的 `PRODUCT_SIZE_PRESETS`、`API_MAX_SIZE` 常量原样搬入并导出（连同类型），`PromptDisplay.tsx` 改为 import。**这是为阶段三的批量生成栏做准备**（那里也要用尺寸下拉），本阶段顺手完成，纯搬移不改内容。`gcd/calcRatioText` 只有 PromptDisplay 用，留在原地不动。

#### 3.3.2 新文件 `frontend/lib/userPrefs.ts`

前端偏好读写层，带模块级缓存（参考 `PromptDisplay.tsx` 中 `_supportsReferenceCache` 的做法——多个组件实例共享一次请求）：

```ts
export interface CustomSizePreset { label: string; width: number; height: number; }
export interface LastSize { preset: string; width: number; height: number; }
export interface UserPrefs {
  custom_products: string[];
  custom_size_presets: CustomSizePreset[];
  last_size: LastSize | null;
}
```

- 模块级 `let _cache: UserPrefs | null` + `let _inflight: Promise<UserPrefs> | null`。
- `fetchPrefs(): Promise<UserPrefs>`：有缓存直接返回；无则发 `GET /api/prefs`（并发去重：多个组件同时挂载只发一次请求）；**失败返回空默认值** `{custom_products: [], custom_size_presets: [], last_size: null}` 并 `console.warn`，绝不抛错阻断页面。
- `addCustomProductIfNew(value: string, knownValues: string[]): void`：value 为空、或已在 knownValues（当前下拉框已有选项）、或已在缓存 custom_products 中 → 直接返回；否则 fire-and-forget `POST /api/prefs/custom-products`，成功后用响应更新 `_cache.custom_products`。**调用方不 await**（不阻塞生成流程），内部 catch 静默。
- `removeCustomProduct(name)` / `addCustomSize(preset)` / `removeCustomSize(label)`：调对应接口，成功后同步更新 `_cache`，返回 Promise 供 UI await 刷新。
- `saveLastSize(last: LastSize): void`：fire-and-forget `PUT /api/prefs/last-size`，同步更新 `_cache.last_size`，catch 静默。

#### 3.3.3 `ProductSelect.tsx` 改造

- 挂载时 `useEffect` 调 `fetchPrefs()` → `setSavedProducts(prefs.custom_products)`。
- 下拉选项合并顺序：`[请选择]` + 规则卡产品选项（props.options）+ **已保存自定义产品**（与 props.options 按 value 精确去重后追加，label 加 `✏️ ` 前缀，value 用原始产品名）+ `[✏️ 自定义...]`。
- 自定义输入模式下，输入框下方渲染"已保存的自定义产品"chips 区（有才显示）：
  - 点 chip → `onChange(该值)` 并退出自定义模式（回到下拉，选中该值）；
  - chip 右侧 `×` → `await removeCustomProduct(name)` → 刷新本地 savedProducts state。
- **保存时机**（与 R5 版本C 自定义值同款策略——生成时落盘）：不在 ProductSelect 内部保存，而是三个版本组件的 `handleGenerate` 各加一行（见下）。

#### 3.3.4 三个版本组件各加一行保存调用

`PromptVersionA.tsx` / `PromptVersionB.tsx` / `PromptVersionC.tsx` 的 `handleGenerate` 里、校验 targetProduct 非空之后：

```ts
addCustomProductIfNew(targetProduct, productOptions.map(o => o.value));
// 版本C 用 (template.product_options || []).map(o => o.value)
```

fire-and-forget，不 await、不影响生成主流程。

#### 3.3.5 `ImageGenSection`（`PromptDisplay.tsx` 内）改造

- **挂载恢复**：`useEffect` 调 `fetchPrefs()`；引入 `touchedRef = useRef(false)`（用户改过任何尺寸控件就置 true）。prefs 返回后若 `!touchedRef.current && prefs.last_size`：
  - `last_size.preset` 在内置预设或自定义预设中存在 → `setSizePreset(preset)`；否则 `setSizePreset('')`（预设已被删，退回手动模式）。
  - 无论 preset 是否命中，`setWidth(last_size.width)`、`setHeight(last_size.height)`（width/height 才是权威值）。
  - 同时 `setCustomPresets(prefs.custom_size_presets)`。
- **下拉合并**：`presetOptions` 的 useMemo 改为依赖 `customPresets`——在内置预设之后追加自定义预设，label 显示 `[自定义] {label} ({width}×{height})`，value 用 `custom:{label}`（前缀防与内置 value 撞车）。`handlePresetChange` 对 `custom:` 前缀的值从 customPresets 查 width/height 填充。
- **记住上次尺寸**：`handlePresetChange` / `handleWidthChange` / `handleHeightChange` 里置 `touchedRef.current = true`，并经 **500ms debounce**（`useRef` 存 timer）调 `saveLastSize({preset: 新值, width, height})`。注意 debounce 里读到的必须是最新值（把新值直接传进 debounce 函数，不读闭包旧 state）。
- **保存当前尺寸为预设**：当 `sizePreset === ''`（手动输入态）时，宽高输入行下方显示 `[💾 保存为常用尺寸]` 按钮 → 点击展开内联小表单（一个名称 input + 确认/取消）→ 确认调 `await addCustomSize({label, width, height})` → 刷新 customPresets 并 `setSizePreset('custom:'+label)`。空名称禁用确认按钮；后端 400（如超长）时 error 显示在小表单旁。
- **删除自定义预设**：当前选中值是 `custom:` 前缀时，下拉旁显示 `[🗑]` 按钮 → `await removeCustomSize(label)` → 刷新列表、`setSizePreset('')`（保留当前 width/height 不动）。
- 注意：A/B/C 三个 PromptDisplay 实例各有一个 ImageGenSection，`fetchPrefs` 模块级缓存保证只发一次请求；`saveLastSize` 同步更新缓存，后挂载的实例能恢复到最新值。三实例间不做实时联动（用户同屏改两处的场景接受 last-write-wins）。

### 3.4 边界与容错

- 后端 prefs 文件损坏 → `load_prefs` 返回默认值，接口不 500；前端拿到空列表照常渲染内置项。
- `GET /api/prefs` 失败（后端没起）→ `fetchPrefs` 返回空默认值，所有持久化功能静默不可用，**不影响任何现有生成/生图流程**。
- 产品名/尺寸 label 含中文、空格、emoji：全链路当普通字符串处理；DELETE 走 query 参数 + `encodeURIComponent`。
- 上限：产品 20 条、尺寸预设 20 条（超出挤掉最旧的）；单条长度产品 100 字符、label 50 字符。

### 3.5 阶段一验收清单

1. 版本B 自定义输入产品"Pillow 抱枕"并生成 → **重启后端 + 硬刷新浏览器** → A/B/C 三版下拉框都出现 `✏️ Pillow 抱枕`；选中它能正常生成。
2. 在自定义模式的 chips 里删除该产品 → 三版下拉不再出现（切 tab 或刷新后验证）。
3. 生图区手动输入 3000×3000 → 保存为预设"抱枕 45x45" → 刷新后下拉出现 `[自定义] 抱枕 45x45 (3000×3000)`，选中正确填充宽高。
4. 选某尺寸后刷新页面重新打开规则卡 → 生图区自动恢复上次的尺寸（预设选中态和宽高值都对）。
5. 删除自定义尺寸预设 → 下拉消失，当前宽高保留；此时 last_size 指向已删预设 → 再刷新恢复为手动模式 + 原宽高（不报错）。
6. 手动把 `data/user_prefs.json` 写成非法 JSON → `GET /api/prefs` 返回默认空值，前端无异常；再保存一个产品 → 文件被重建为合法 JSON。
7. 连续添加 21 个产品 → 只保留最新 20 个。
8. 回归：不做任何偏好操作时，A/B/C 生成提示词、生图提交行为与改造前完全一致；`npm run lint` 通过。

### 3.6 阶段一收尾动作

- CLAUDE.md 增补：user_prefs 存储模式（锁+原子写、无 ID 机制）、前端 `lib/userPrefs.ts` 模块级缓存约定、`lib/sizePresets.ts` 为共享单一事实来源。
- 本文档 §9 工单追加完成记录。

---

## 4. 阶段二：批量上传分析（需求4）

### 4.1 目标与现状

**目标**：一次选择/拖拽多张竞品图 → 填一次全局分析方向（hint）→ 串行逐张分析 → **每张成功后自动保存进规则库** → **完成条目收起为紧凑行，队列自动继续** → 全部完成显示汇总。失败单张可重试、不阻塞队列。

**现状（代码事实）**：
- `frontend/components/ui/FileUpload.tsx`：单文件（input 无 multiple，drop/paste 只取第一个文件）。
- `frontend/app/analyze/page.tsx`：单图状态机 `idle → ready → analyzing → done → saved → error`。
- `backend/routers/analyze.py` `POST /api/analyze`：单文件同步接口，阻塞约 30~60s（内部 SABC+6层拆解两个 VLM 请求已并行）。
- `backend/routers/rules.py` `create_rule`：已有规则 ID 撞车重分配保护（#25），串行批量保存无并发风险。

**后端本阶段零改动。** 全部工作在前端。

### 4.2 设计

#### 4.2.1 `FileUpload.tsx` 改造（向后兼容）

- 新增可选 props：`multiple?: boolean`（默认 false）、`onFilesSelect?: (files: File[]) => void`。
- `multiple=true` 时：`<input multiple>`；`handleChange`/`handleDrop` 收集**全部** `image/*` 文件调 `onFilesSelect(files)`（含只选 1 张的情况，也走 `onFilesSelect([f])`，由父组件决定单/批分流）；粘贴事件一次通常只有一张图，同样包装成数组回调。
- `multiple` 未传时行为与现状完全一致（其他调用处不受影响；实施时 grep `FileUpload` 的全部引用确认）。
- 提示文案在 multiple 模式下改为"支持一次选择/拖入多张图片"。

#### 4.2.2 `analyze/page.tsx` 批量模式

**状态扩展**：`PageState` union 增加 `'batch'`。新增：

```ts
interface BatchItem {
  id: number;                 // 递增序号，React key
  file: File;
  preview: string;            // Object URL（重置时 revoke，沿用现有防泄漏模式）
  status: 'queued' | 'analyzing' | 'saved' | 'failed';
  ruleId?: string;            // 保存后
  ruleName?: string;
  reuseLevel?: string;        // SABC
  error?: string;
}
const [batchItems, setBatchItems] = useState<BatchItem[]>([]);
const [batchPhase, setBatchPhase] = useState<'ready' | 'running' | 'done'>('ready');
const stopFlagRef = useRef(false);      // 停止队列标记
```

**入口分流**：`FileUpload` 传 `multiple` + `onFilesSelect`：
- `files.length === 1` → 走现有单图 `handleFileSelect(files[0])`（**单图全流程不动**，包括预览确认、保存后原地展开提示词生成）。
- `files.length >= 2` → 构建 batchItems（**上限 20 张**，超出丢弃并提示"单批最多 20 张，已保留前 20 张"），`setPageState('batch')`、`setBatchPhase('ready')`。

**batch/ready 界面**：缩略图网格（小图 + 文件名）+ 全局 hint textarea（复用现有样式与 1000 字限制，说明"将应用到本批所有图片"）+ `[开始批量分析 (N 张)]` + `[重新选择]`（revoke 所有 preview 后回 idle）。

**串行执行循环**（`batchPhase='running'`）：

```
for 每个 status==='queued' 的 item（按序）:
  if (stopFlagRef.current) break
  置 item.status='analyzing'
  try:
    FormData: file + hint（非空才 append，同单图逻辑）
    res = await apiUpload('/api/analyze', formData)
    data = unwrapData(res); card = data.rule_card || data
    if (!card || card.parse_error) throw Error(card?.parse_error || '分析失败')
    // 自动保存——与单图 handleSave 完全同款字段拼装：
    saveRes = await apiPost('/api/rules', { ...card, thumbnail_path: getImageUrl(data.uploaded_image) || '' })
    saved = unwrapData(saveRes)
    置 item: status='saved', ruleId=saved?.rule_id || card.rule_id, ruleName=card.rule_name, reuseLevel=card.reuse_level
  catch (err):
    置 item: status='failed', error=msg
    continue   // 单张失败不阻塞队列
循环结束 → setBatchPhase('done')
```

- **必须串行**（并发=1）：`ImageAnalyzer.analyze` 内部已是 2 路 VLM 并行，前端再并发会叠成 4~6 路，容易撞上游限流。此为本阶段固定决策，不做并发开关。
- setState 更新单个 item 用函数式 `setBatchItems(prev => prev.map(...))`，避免闭包旧值。
- 当前分析中的 item 显示实时计时（沿用现有 `elapsed` 计时思路，per-item 起算）。

**列表展现（决策点 4：完成即收起）**：
- `saved`/`failed` 项渲染为**单行紧凑行**：`[24px 缩略图] 文件名 → 规则名 [SABC badge] [📝 去编辑]`（链接 `/rules/{ruleId}`，`target="_blank"` 免打断队列页）；失败行：`[24px 缩略图] 文件名 [❌ 失败原因（truncate，title 显全文）] [🔄 重试]`。
- `analyzing` 项渲染为**展开卡**：较大缩略图 + spinner + "分析中… 已等待 Ns（约 1 分钟）"。
- `queued` 项：置灰单行"排队中"。
- 顶部进度条/计数：`已完成 X / 失败 Y / 共 N`；`running` 期间显示 `[⏹ 停止队列]`（置 stopFlagRef，当前张跑完后停，剩余保持 queued）与红字提示"批量分析进行中，请勿关闭本页"。

**重试**：仅 `batchPhase !== 'running'` 时可点。点击将该 item 置回 `queued` → `setBatchPhase('running')` → 复用同一个执行循环（循环本身就是"处理所有 queued 项"，天然支持）。

**done 界面**：汇总条（`✅ 成功 X 张已存入规则库，❌ 失败 Y 张`）+ `[🔄 再来一批]`（revoke 全部 preview、清空、回 idle）+ `[📋 查看规则库]` 链接。失败项留在列表可继续重试。

**beforeunload 守卫**：`useEffect` 在 `batchPhase === 'running'` 时挂 `beforeunload`（`e.preventDefault()`），离开 running 或组件卸载时移除。

### 4.3 边界与已知限制（向用户明示）

- 浏览器侧队列：关闭标签页队列即停，已保存的不受影响；正在分析中的那张，后端会继续跑完但结果无人接收（该张需重传）。本地单机工具可接受，页面有醒目提示。
- 同图重复上传不做去重（规则库允许多张相似卡，删除在规则库操作）。
- 非图片 MIME 的文件：前端 `image/*` 过滤 + 后端白名单双保险，被拦的张标 failed 不阻塞。
- 批量模式不做逐张人工预览确认（决策点 4：自动保存）；需要精修的卡走"去编辑"链接。

### 4.4 阶段二验收清单

1. 一次拖入 4 张图（含 1 张大图 AVIF/HEIC）→ 填一次 hint → 开始 → 4 张严格串行分析，每张完成后条目立即收起为紧凑行、下一张自动开始。
2. 全部完成后规则库出现 4 张新卡（缩略图正常、SABC 正常）；hint 生效（抽 1 张对比分析结果侧重）。
3. 中途在第 2 张分析时点"停止队列" → 第 2 张跑完后停止，第 3/4 张保持排队；再点开始可继续。
4. 人为制造 1 张失败（如断开 AI 配置或传损坏文件）→ 该张标失败+原因，队列继续；结束后点"重试"单独重跑成功。
5. 批量进行中刷新/关闭页面 → 浏览器弹确认框。
6. 只选 1 张图 → 完整走现有单图流程（ready 确认 → 分析 → 预览 → 手动保存 → 原地展开提示词生成），与改造前无差异。
7. 超选 25 张 → 提示并只保留 20 张。
8. `npm run lint` 通过。

### 4.5 阶段二收尾动作

- CLAUDE.md 增补：批量分析为前端串行队列（并发=1 的理由：analyzer 内部已 2 路并行）、自动保存策略、单图流程不变的分流条件。
- 本文档 §9 工单追加。

---

## 5. 阶段三：规则库批量删除（追加需求5） + 版本B 多套设计方案（需求3）

> **2026-08-17 追加需求（用户验收阶段二时提出）**：规则库增加批量删除功能，归入阶段三。
> 阶段三内部顺序仍遵循"先小后大"：**§5.0 批量删除（小、与版本B 无耦合）→ §5.1 起 版本B 多方案（大）**。
> 两项各自独立验收。

### 5.0 规则库批量删除（追加需求5）

#### 5.0.1 目标与现状

**目标**：规则库列表页支持"进入选择模式 → 勾选多张（含全选/反选当前筛选结果）→ 一次删除"，删除前二次确认并明示不可恢复；结果分条反馈成功/失败数。

**现状（代码事实）**：
- `frontend/app/rules/page.tsx`：拉 `GET /api/rules` → 传给 `RuleCardList`，`onDeleted(ruleId)` 回调从本地数组移除（不重拉列表）。
- `frontend/components/rules/RuleCardList.tsx`：每张卡是 `<Link href={/rules/{id}}>` 包 `<Card>`，hover 时右上角显现单张删除按钮 `🗑`（`handleDelete` 里 `e.preventDefault()` + `e.stopPropagation()` 阻止触发外层 Link 跳转 —— **勾选交互必须沿用这个防跳转处理**）。已有筛选（SABC）、搜索（规则名+核心卖点）、排序三个条件，`filteredRules` 是它们的结果。
- `backend/routers/rules.py`：`DELETE /api/rules/{rule_id}` 单条删除，404 守卫。
- `backend/services/rule_store.py :: delete_rule(rule_id)`：**已正确实现"先读 JSON 取 `source_images`/`thumbnail_path` → 再删 JSON/SQLite/清 uploads 文件"**（顺序关键，CLAUDE.md 已记录），返回 bool。

#### 5.0.2 后端设计

新增 `POST /api/rules/batch-delete`（`routers/rules.py`）。**不用 `DELETE` + body**——部分代理/客户端对带 body 的 DELETE 支持不一致，且这是"批量操作"语义，用 POST 子资源路径更稳。

- 请求体（pydantic v2）：`{"rule_ids": List[str]}`。
- 校验：空列表 → 400 `"请至少选择一条规则卡"`；**上限 100 条** → 400（防误传超大列表把请求跑成分钟级）。
- 逻辑：**串行**逐个调已有的 `delete_rule(rule_id)`（复用单一事实来源，不重写删除逻辑）。单条失败（抛异常）不中断整批，记入 `failed` 并 `logging.exception`；`delete_rule` 返回 False（不存在）记入 `not_found`。
- 响应（统一包装格式）：
  ```json
  {"success": true, "data": {
    "deleted": ["RULE-0059", "RULE-0060"],
    "not_found": ["RULE-9999"],
    "failed": [{"rule_id": "RULE-0061", "error": "..."}]
  }}
  ```
- **为什么串行不加锁**：`delete_rule` 是"删文件 + 删 SQLite 行"，`unlink(missing_ok=True)` 与 `DELETE WHERE` 都是幂等操作，重复删不炸；且单进程部署下本请求内串行执行，无需额外锁（与 `rule_store` 现有约定一致——**它本身就没有锁，ID 生成走单请求 scan-max**）。
- except 块用 `logging.exception`；未预期异常 → 500。

#### 5.0.3 前端设计

**`RuleCardList.tsx`** 增加选择模式（不新建组件，删除交互与单张删除同处维护）：

- 新 state：`selectMode: boolean`（默认 false）、`selectedIds: Set<string>`、`batchDeleting: boolean`、`batchResult: string`（结果提示文案）。
- 工具栏：
  - 非选择模式：现有搜索/排序/筛选之外，追加 `[☑️ 批量删除]` 按钮（列表为空时不显示）。
  - 选择模式：切换为一行操作条——`已选 K 条` + `[全选当前 N 条]`/`[取消全选]` + `[反选]` + `[🗑 删除所选 (K)]`（K=0 禁用，`variant="danger"`）+ `[取消]`（退出并清空勾选）。
  - **"全选"只作用于 `filteredRules`（当前筛选/搜索结果），不是全部 rules**——用户按 C 级筛选后点全选，预期是"选中这些 C 级卡"。按钮文案显式写出条数避免误解。
- 卡片：选择模式下
  - 左上角渲染勾选框（复用 `PromptVersionA` 推荐图网格同款的自绘 checkbox 样式，保持视觉一致）；
  - **整卡点击改为切换勾选而不是跳转**——外层 `<Link>` 在选择模式下换成 `<div>`（不是靠 `preventDefault` 拦，直接不渲染 Link，语义更干净，避免"看起来是链接但点了不跳"的可访问性问题）；
  - 已选卡加高亮边框（`border-codex-accent` + ring，同版本A 选中态）；
  - 单张 `🗑` 按钮在选择模式下隐藏（避免两套删除入口并存造成误触）。
- 删除流程：点 `[🗑 删除所选]` → `confirm()` 二次确认（文案含条数 + "此操作不可恢复，同时会删除关联的竞品原图"）→ `POST /api/rules/batch-delete` → 用响应的 `deleted` 数组逐个调 `onDeleted?.(id)` 同步父组件数据源 → 退出选择模式 → 显示结果提示条（`✅ 已删除 X 条`，有 `failed`/`not_found` 时追加 `⚠️ Y 条失败`，5 秒后自动消失或点 × 关闭）。
- **失败项不从列表移除**（只移除 `deleted` 里的），用户能看到哪些还在、可重试。

#### 5.0.4 边界与容错

- 勾选后筛选条件变化：`selectedIds` 保留（不因筛选变化清空）——但"删除所选"的确认文案用 `selectedIds.size`，用户看到的条数始终是真实待删数；若担心误删被筛掉的项，`[全选当前 N 条]` 的语义已明示范围。
- 后端不可用/网络失败：显示错误提示条，列表不动（不做乐观更新，避免"界面删了实际没删"）。
- 批量删除进行中禁用所有删除按钮（`batchDeleting`），防重复提交。
- 上限 100 条：前端在确认前也校验并提示，不依赖后端报错。

#### 5.0.5 §5.0 验收清单

1. 点「批量删除」进入选择模式 → 卡片出现勾选框、单张 🗑 隐藏、点卡片是勾选而非跳转。
2. 勾 3 条 → 计数显示"已选 3 条" → 删除 → 二次确认 → 列表少 3 条、提示"已删除 3 条"；刷新页面确认真的少了 3 条（后端已落地）。
3. 被删规则卡的关联文件已清理：删除前记下某卡的 `thumbnail_path` 文件名，删除后 `backend/data/uploads/` 里该文件不存在、`data/rules/{id}.json` 不存在、SQLite 里查不到。
4. 按 C 级筛选 → `[全选当前 N 条]` 只选中 C 级那 N 条（切回全部后确认其他等级未被选中）。
5. 反选按钮正确取反当前筛选结果内的勾选状态。
6. K=0 时删除按钮禁用；点「取消」退出选择模式并清空勾选。
7. 混合场景：勾 2 条，其中 1 条先在另一个标签页删掉 → 批量删除返回 `deleted:1, not_found:1` → 界面移除成功那条、提示含失败/不存在计数、列表不整体崩。
8. 单张删除（原有功能）与列表筛选/搜索/排序全部回归正常；`npm run lint` 与基线一致。

#### 5.0.6 §5.0 收尾动作

- CLAUDE.md 增补：`POST /api/rules/batch-delete` 端点约定（复用 `delete_rule` 单一事实来源、串行、部分失败不中断的响应形状）、前端选择模式的"全选只作用于筛选结果"与"选择模式下不渲染 Link"两条约定。
- 本文档 §9 工单追加。

---

### 5.1 版本B 多套设计方案：目标与现状

**目标**：版本B 配置区选择"推荐方案数"（1~4，默认 3）→ AI **一次调用**返回 N 套彼此差异化的改款方案 → 前端以方案卡片列表展示，点选切换 → **当前方案仍用现有 PromptDisplay 完整渲染（决策点 3：展现形式不变，不破坏 A/B/C 三栏布局）** → 新增"批量生成勾选方案"栏，勾 K 套提交 K 个生图任务（每套 1 张）。

**现状（代码事实）**：
- `backend/prompts/prompt_generation.py` `get_recommendation_prompt(rule_card_json, target_product)`：prompt 明确"推荐**一个**改款方向"，输出单套 JSON（recommended_changes/reason/三段英文描述/negative_elements/customization_slots）。
- `backend/services/prompt_generator.py`：
  - `generate_version_b()`（74~175 行）：单套流程——`_ai_recommend` 或 `_random_recommend` → `_extract_customization_slots` → `_get_adaptation` → `_build_structured_prompt_cn` → `_build_image_prompts` → `_build_change_summary` → `recommended_changes_detail` 加工 → `_build_edit_prompt`，返回单个结果 dict。
  - `_ai_recommend()`（781 行）：调 `_call_ai_for_json`（**不传 temperature**，#13 创意任务约定），失败降级 `_random_recommend`。
  - `_random_recommend()`（808 行）：每维度取 `alternatives[0]`。
- `backend/routers/prompts.py` `GenerateBRequest`：`{rule_id, target_product}`。
- `frontend/components/prompts/PromptVersionB.tsx`：单 result → 单 PromptDisplay。
- `services/ai_client.py` `MAX_TOKENS=8192`。

### 5.2 后端设计

#### 5.2.1 `prompts/prompt_generation.py`：`get_recommendation_prompt` 加参数

签名改为 `get_recommendation_prompt(rule_card_json: str, target_product: str, num_directions: int = 1) -> str`。

- `num_directions == 1`：**返回与现有完全相同的文本**（默认路径零回归——保留现函数体不动，多方向走新分支）。
- `num_directions > 1`：新分支文本，要求：
  - 保留现有 6 条要求（核心卖点不动、只从第3层选、应用第4层适配、理由、描述具体可生图、可定制项判断）；
  - 新增要求："输出 {num_directions} 套方案，**各套之间改款方向必须明显差异化**——体现在不同的替换元素组合、不同的风格/色彩/氛围路线，不允许多套方案只是同一维度换近义词"；
  - `customization_slots` 与方案无关，**在顶层输出一份**（不要每套重复）；
  - JSON 输出格式（**示例值全部用方括号占位符，铁律 §2-6**）：

```
{
    "recommendations": [
        {
            "recommended_changes": {"维度名": "[从该维度 alternatives 中选择的替代方案]"},
            "reason": "[这套方案的推荐理由，说明差异化思路]",
            "style_description": "[英文风格描述]",
            "color_description": "[英文色彩描述]",
            "layout_description": "[英文构图描述]",
            "negative_elements": ["[需要排除的元素]"]
        }
    ],
    "customization_slots": [ 与现有单套格式相同的占位符结构 ]
}
```

#### 5.2.2 `prompt_generator.py` 改造

**a) 抽取单套组装 helper（纯重构，不改逻辑）**

把 `generate_version_b` 中从 `recommended_changes = recommended.get(...)` 到 `return {...}` 的整段组装逻辑抽成：

```python
def _assemble_version_b_direction(
    self,
    rule_card: dict,
    recommended: dict,
    customization_slots: list,
    adaptation: dict,
    target_product: str,
    library_recommendations: Optional[List[dict]] = None,
) -> dict:
    """按现有 generate_version_b 的组装逻辑原样产出单套结果 dict（形状与现有返回完全一致）。"""
```

内部依次：`_build_structured_prompt_cn` → `_build_image_prompts` → `_build_change_summary` → `recommended_changes_detail` 加工（original 只从 layer_3 取，#2 约定不变）→ `image_prompt_edit`（`_edit_value_for_original/_chosen` 语言映射不变）→ 返回含 `customization_slots` 的完整 dict。**返回形状必须与现有单套返回逐字段一致**（前端 PromptDisplay 直接消费）。

**b) 多方案推荐**

```python
async def _ai_recommend_multi(self, rule_card: dict, target_product: str, num_directions: int) -> dict:
    """一次 AI 调用返回多套方案。返回 {"recommendations": List[dict], "customization_slots": Optional[list], "degraded": bool}"""
```

- `_call_ai_for_json(system_prompt, user_prompt)`，**不传 temperature**（创意任务）；user_prompt 写明"推荐 {num_directions} 套差异化方案"。
- 解析：`result.get("recommendations")` 必须是 list；逐项过滤——`recommended_changes` 不是非空 dict 的项丢弃。有效项 ≥1 即采用（不足 num_directions 记 `logging.warning` 并照常返回，不报错）；有效项为 0 或调用失败 → 降级：

```python
def _random_recommend_multi(self, layer_3: dict, target_product: str, num_directions: int) -> List[dict]:
    """降级/无 AI 模式：第 i 套方案每个维度取 alternatives[i % len(alternatives)]（空 alternatives 用 original）。
    reason 注明轮转降级。天然保证各套差异（只要 alternatives 数量>1）。"""
```

**c) `generate_version_b` 签名与返回**

```python
async def generate_version_b(self, rule_card, target_product,
                             library_recommendations=None, num_directions: int = 1) -> dict:
```

- `num_directions == 1`：**走现有代码路径原样返回单套 dict**（唯一变化是组装段改调 `_assemble_version_b_direction`，行为等价）。
- `num_directions > 1`：
  - 有 AI：`multi = await self._ai_recommend_multi(...)`；`customization_slots = self._extract_customization_slots(rule_card, multi.get("customization_slots"))`（AI 顶层 slots 缺失传 None 走兜底，与现有 R4 语义一致）。
  - 无 AI：`recommendations = self._random_recommend_multi(...)`；slots 走 `_extract_customization_slots(rule_card, None)`。
  - `adaptation = self._get_adaptation(layer_4, target_product)` 算一次共用。
  - 逐套调 `_assemble_version_b_direction`（同一份 slots 塞进每套结果——PromptDisplay 无需改动即可展示可定制项）。
  - 返回 `{"directions": [套1, 套2, ...], "num_directions": len(...)}`。

#### 5.2.3 `routers/prompts.py`

- `GenerateBRequest` 加字段：`num_directions: int = Field(default=1, ge=1, le=4)`（pydantic v2；`from pydantic import BaseModel, Field`）。
- `generate_version_b` 路由透传 `num_directions=request.num_directions`。响应包装与 `ai_mode` 字段不变。

#### 5.2.4 Token 截断风险验证（实施内步骤，非可选）

用一张真实规则卡按 `num_directions=4` 实测：后端日志无 JSON 解析失败、4 套 recommendations 完整（最后一套的 negative_elements 数组闭合正常）。若出现截断：把上限降为 3（`le=3` + 前端选项去掉 4 + prompt 文案同步），**并在本文档记录该变更（日期+理由）**。不要为此全局调大 `MAX_TOKENS`（影响所有 AI 调用的费用与延迟）。

> **实测结论（2026-08-17）：N=4 未出现截断，上限 `le=4` 保持不变。**
> 用 RULE-0061（带 `alternatives_en` 的新卡）+ gpt-5.4-mini 实测：4 套 recommendations 全部完整、最后一套各字段闭合正常、无 JSON 解析失败、无降级、4 套组合互不相同。耗时参考：N=1 约 16s、N=3 约 22s、N=4 约 28s。`MAX_TOKENS=8192` 未动。

### 5.3 前端设计

#### 5.3.1 前置小重构：`supports_reference` 缓存提取

`PromptDisplay.tsx` 中模块级 `_supportsReferenceCache`（供 useReference 默认值用）提取到新文件 `frontend/lib/genConfig.ts`：`getSupportsReference(): Promise<boolean>`（模块缓存 + 并发去重），`PromptDisplay.tsx` 改为 import 使用（行为不变）。阶段三批量生成栏也要用它。

#### 5.3.2 `PromptVersionB.tsx` 改造

**配置区**（现有 CollapsibleSection "🔧 目标产品" 内）：
- ProductSelect 下方加"推荐方案数"按钮组 `[1] [2] [3] [4]`（样式复用 ImageGenSection 的 count 按钮组），state `numDirections` 默认 **3**；旁注小字"方案越多 AI 耗时越长"。
- `handleGenerate` 请求体加 `num_directions: numDirections`。

**响应分流**：

```ts
const data = unwrapData<any>(res);
if (Array.isArray(data.directions)) {
  setDirections(data.directions); setActiveIdx(0);
  setCheckedIdx(new Set(data.directions.map((_, i) => i)));  // 默认全勾选
  setResult(null);
} else {
  setResult(data); setDirections(null);   // N=1：与现状完全一致的单套渲染
}
```

**多方案渲染**（`directions` 非空时，在配置区下方、按顺序）：

1. **方案卡片列表**（窄栏内垂直堆叠）。每张卡：
   - 左上角 checkbox（批量生成勾选，独立于"当前查看"）；
   - 标题 `方案 N`；激活卡高亮边框（`border-codex-accent`），点击卡体 `setActiveIdx(n)`；
   - `reason` 摘要（`line-clamp-2`，title 显全文）；
   - 改动概览：取 `direction.recommended_changes_detail` 前 3 条渲染 `维度: original → changed_to`（original 空则只显 changed_to，沿用 `change.original &&` 守卫），多于 3 条加"等 X 处改动"。
2. **当前方案的 PromptDisplay**（决策点 3 的落点）：

```tsx
<PromptDisplay
  key={activeIdx}   // 关键：切换方案强制重挂载，editable/勾选等内部 state 全部重置为该方案的初始值
  result={directions[activeIdx]}
  ruleId={ruleId} ruleName={ruleCard?.rule_name} version="B"
  hasRuleImage={...} ruleImageUrl={...}   // 与现有单套调用相同
/>
```

   卡片列表下方加一行小字提示："切换方案会重置该方案下的手动编辑"。**PromptDisplay 本身零改动**——每套方案的中文结构化提示词、生图提示词、可定制项、单方案生图区的展现与操作和现在完全一样。
3. **批量生成栏**：新增 `CollapsibleSection`（`title="🚀 批量生成勾选方案"`，defaultExpanded=false），内容：
   - 勾选计数行：`已勾选 K / N 套（在上方方案卡片勾选）`；
   - 尺寸下拉：复用 `lib/sizePresets.ts` 内置预设 + `fetchPrefs()` 的自定义预设（与 ImageGenSection 同款合并逻辑，可抽一个共用的 options 构建函数放 `lib/sizePresets.ts`）；默认值取 `prefs.last_size`（无则 square）；提交前同样按 `API_MAX_SIZE=1600` 等比缩放（复用现有缩放逻辑，可一并抽到 `lib/sizePresets.ts` 导出 `clampToApiMax(w,h)`）；
   - "附带竞品原图（编辑指令模式）" toggle：默认值 = `hasRuleImage && supportsReference && 所有勾选方案都有 image_prompt_edit`；不可用时置灰+原因文案（无竞品原图 / 后端无编辑提示词 / 当前生图接口不支持带图——镜像批次四 useReference 开关的三种置灰原因）；
   - `[🎨 生成勾选的 K 套（每套 1 张）]` 按钮：**串行**循环勾选方案，逐套 `apiPost('/api/gen/submit', {...})`：

```ts
{
  rule_id, rule_name, version: 'B', count: 1,
  width, height,               // 缩放后的值
  prompt_positive: useRef模式 ? d.image_prompt_edit : d.image_prompt_positive,
  prompt_negative: d.image_prompt_negative || '',
  attach_rule_image: useRef模式,
  reference_image_paths: [],
}
```

   - 每套的提交状态行：`方案 N：提交中… / ✅ 已提交（task_xxx） / ❌ 失败原因`；全部提交完显示"已提交 K 个任务，请到「生图任务」页查看进度与结果"（链接 `/gen`）。**批量栏不做轮询与图片展示**（范围裁剪，明确记录：单方案精细生图走各方案自己的 PromptDisplay 生图区，那里有完整轮询/预览/参考图上传）。
   - **明示限制**（栏内小字）：批量生成使用各方案的原始提示词，不包含"可定制项勾选"与手动编辑内容；需要这些请切到对应方案在其生图区单独生成。

**兼容性**：`numDirections=1` 时整条链路（请求→响应形状→渲染）与现状 100% 一致；`analyze/page.tsx` 保存后原地展开的 PromptTabs 里的版本B 同样获得新能力（组件复用，无需额外改动）。

### 5.4 阶段三验收清单

1. **回归**：方案数=1 生成 → 响应为旧单套形状，渲染、编辑、可定制项、生图与改造前完全一致。
2. 方案数=3 生成 → 3 张方案卡，人工确认三套 `recommended_changes` 组合明显不同（不是同维度近义词）；卡片改动概览与展开后 PromptDisplay 的「🎯 AI推荐改动」卡片一致。
3. 切换方案卡 → 下方 PromptDisplay 完整重置为该方案内容（中文结构化提示词、生图提示词、可定制项都对应变化）；展现形式与单套模式完全相同；A/B/C 三栏布局不变，版本C 同屏可正常操作。
4. 勾选 3 套批量生成 → 生图任务页出现 3 个 version=B 任务，出图为三种不同设计；"附带竞品原图"开时任务 `used_reference=true` 且提示词为编辑指令式。
5. 方案数=4 实测无 JSON 截断（后端日志确认）；若截断按 §5.2.4 处理并记录。
6. 断开 AI（临时清 config.json 的 api_key）→ 方案数=3 走随机轮转降级：3 套方案各维度取不同 alternatives，reason 注明降级；可定制项走规则卡兜底。
7. `npm run lint` 通过。

### 5.5 阶段三收尾动作

- CLAUDE.md 增补：版本B 多方案的请求/响应形状约定（N=1 兼容旧形状）、`key={activeIdx}` 重挂载重置策略、批量生成栏的范围边界（不含可定制项/手动编辑/轮询）、`_random_recommend_multi` 轮转降级。
- 本文档 §9 工单追加。

---

## 6. 阶段四：元素拆分图（需求1，Spike 先行）

### 6.1 目标与现状

**目标**：版本C 面板加"🧩 元素拆分图"折叠区：列出规则卡识别出的图案元素（去重、标注文字位）→ 用户勾选 → 逐元素调用带参考图的编辑式生图，产出"该元素单独置于干净背景上的素材图" → 完成后勾选打包下载 zip。

**现状（代码事实）**：
- 唯一已验证的"从原图操作元素"手段：OpenAI 模式 `/v1/images/edits`（批次三 Spike 确认真实参考输入图；`REFERENCE_SUPPORT = {"openai": True, "aireiter": False}`）。
- 元素数据源：`layer_3.replaceable_elements`（dim → {original, original_en, alternatives, is_text_slot}）+ `layer_2.must_have_elements`（[{slot, description, description_en, position, is_text_slot}]）。两层常重复记录同一元素（`_get_pod_hints` 已有成熟去重先例）。
- 生图任务管道完备：`POST /api/gen/submit`（attach_rule_image=true 即自动带竞品原图、白名单校验、任务落库）、任务页按 version 分 tab（`frontend/app/gen/page.tsx` 的 `VERSION_TABS`，未知 version 落"未标记版本"）、`download_images` 已支持 data URI 落盘。
- **本阶段生图侧几乎零新增后端逻辑**：前端逐元素调用现有 submit 接口即可，新增的只有"元素清单+抠取指令"端点和 zip 打包端点。

### 6.2 Spike（开工第一步，半天，结论决定 go/no-go）

**前置**：生图配置为 openai 模式（`supports_reference=true`）。

**步骤**：
1. 从规则库取一张元素丰富的已有竞品图（如腊肠犬毯/万圣节图）。
2. 写临时脚本（或 curl）直接调 `ImageGenClient.submit_task(prompt=..., reference_images=[竞品图])`，对 2~3 个元素各试两种指令：
   - 白底版：`Extract only the [element description] from the provided image. Reproduce it exactly as it appears in the image — same style, same colors, same details. Place it alone, centered, on a plain pure white background. Remove all other elements, all text, and the original background.`
   - 透明底版：同上，把背景句换成 `on a fully transparent background`，输出 PNG 后检查是否真有 alpha 通道（Pillow 打开看 mode/extrema）。
3. 逐张人工比对三项判定标准：
   - **保真**：元素样式/配色/细节与原图一致，不是"重新想象的同类物"；
   - **干净**：无其他元素、文字、背景残留；
   - **透明底**：alpha 通道真实存在（不支持则降级为白底交付，记录结论）。

**产出**：Spike 结论写入本文档 §6.7 + CLAUDE.md（格式参照"生图参考图支持（批次三 Spike 结论）"章节：结论、反例、对实现的影响），定稿指令模板。**若保真/干净不达标 → 停止本阶段，报告用户重新决策，不得擅自换方案。**

### 6.3 后端设计

#### 6.3.1 元素清单端点：`GET /api/prompts/elements/{rule_id}`（`routers/prompts.py`）

新增 service 函数 `prompt_generator.py :: extract_element_list(rule_card: dict) -> List[dict]`（同步纯函数，可不放 PromptGenerator 类里、独立函数即可）：

**收集顺序与去重规则**（复用 `_get_pod_hints` 的两道防线思路）：
1. 遍历 `layer_3.replaceable_elements`：每个 (dim, item) 产出一条：
   - `name_cn = dim`、`value_cn = item.original`；
   - `value_for_prompt`：优先 `item.original_en`（非空），否则放行 `item.original`（**允许中文**——批次四 `_edit_lang_value` 同款策略：中文只是给模型定位图里的元素，模型同时看得到图）；
   - `is_text_slot = _parse_is_text_slot(item.get("is_text_slot"))`（防字符串 "false" 误判）。
2. 遍历 `layer_2.must_have_elements`：每个 el：
   - **去重防线①**：el.description 包含任一已收集条目的 `value_cn` 或 `value_for_prompt` → 跳过（两层记录的是同一元素）；`slot` 名与已收集 `name_cn` 相同 → 跳过；
   - 产出：`name_cn = el.slot`、`value_cn = el.description`、`value_for_prompt = el.description_en or el.description`、`position = el.position`、`is_text_slot` 同上解析。
3. **去重防线②**：全列表按 `value_for_prompt` 保序去重。
4. 排序：非文字位在前，文字位排最后。
5. 每条生成 `extraction_prompt`：模块级常量模板（**Spike 定稿后填入**，先用 §6.2 白底版占位）`ELEMENT_EXTRACTION_PROMPT_TEMPLATE.format(element=value_for_prompt)`。模板放 `prompts/prompt_generation.py`（与其他提示词模板同处维护）。

**路由**：读规则卡（404 守卫），返回：

```json
{"success": true, "data": {"elements": [
  {"element_key": "...", "name_cn": "...", "value_cn": "...", "position": "",
   "is_text_slot": false, "extraction_prompt": "Extract only the ..."}
]}}
```

`supports_reference` 与竞品原图有无由前端自行判断（`getSupportsReference()` + `ruleCard.source_images`），端点不重复返回。

#### 6.3.2 zip 打包下载端点：`POST /api/gen/download-zip`（`routers/image_gen.py`）

- 请求体（pydantic）：`{"task_ids": List[str]}`，上限 20（超出 400）。
- 逻辑：逐 task：`get_task` 不存在则跳过记 skip；`task.local_images` 为空则 `await download_images(task_id)`（已支持 data URI 落盘）；收集本地文件路径。全部收集完为空 → 400 "没有可下载的图片"。
- 打包：`io.BytesIO` + `zipfile.ZipFile(..., "w", zipfile.ZIP_STORED)`（图片已压缩，STORED 即可）；zip 内文件名用 `{task_id}_{原文件名}` 防重名。
- 响应：`StreamingResponse`（`from fastapi.responses import StreamingResponse`），`media_type="application/zip"`，header `Content-Disposition: attachment; filename=split_elements_{时间戳}.zip`。
- **注意**：这是裸二进制响应，不走包装格式（下载类端点例外，文档在此记录）；前端**不能用 `apiPost`**（会 `response.json()` 崩），必须直接 `fetch` 取 `blob`。

#### 6.3.3 生图任务复用（无新端点）

前端逐元素调**现有** `POST /api/gen/submit`：

```json
{"rule_id": "...", "rule_name": "...", "version": "E",
 "prompt_positive": "<该元素的 extraction_prompt>", "prompt_negative": "",
 "width": 1024, "height": 1024, "count": 1,
 "attach_rule_image": true, "reference_image_paths": []}
```

- `attach_rule_image=true` 自动走已有链路：取 `rule.source_images[0]` → `prepare_image_for_vlm` → `/v1/images/edits`。
- submit 会前置 `[Canvas: 1:1 square format]` 比例句——对抠取任务无害，不做特殊处理。
- OpenAI 模式该接口**同步阻塞至出图**（最长 300s），前端按顺序 await 即天然串行 + 状态天然可见。

### 6.4 前端设计

#### 6.4.1 新组件 `frontend/components/prompts/ElementExtractSection.tsx`

Props：`{ ruleId: string; ruleCard: any }`。挂载位置：`PromptVersionC.tsx` 最底部（生成结果区之后），独立 `CollapsibleSection`（`title="🧩 元素拆分图（衍生）"`，`defaultExpanded=false`）——**不依赖版本C 是否已生成提示词**，只要有规则卡即可用。

> **【2026-08-17 变更（用户补充需求，实施前）】入口从"默认渲染的折叠区"改为按钮触发，上段的入口设计以本块为准：**
> - **理由**：元素拆分是低频功能，只有个别产品会用到，不应默认占据界面注意力，也不应在未使用时发出任何请求。
> - 默认只渲染一行轻量入口按钮 `[🧩 拆分图案元素（按需）]`（ghost/secondary 样式）+ 灰字小注"低频功能：仅个别产品需要拆元素素材"。内部 `activated` state 初始 false，**未激活时零请求、不渲染面板**。
> - 无竞品原图（`!ruleCard?.source_images?.length`，纯 props 判断、零请求）→ 入口按钮直接置灰 + 提示"该规则卡没有竞品原图，无法拆分"。
> - 点击按钮 → `activated=true` → **此时才**并行取元素清单 + `getSupportsReference()`，渲染完整面板（面板外层可继续用 CollapsibleSection 包裹，激活后默认展开；收起再展开不重复请求）。
> - 其余门控（接口不支持带图 / 元素清单为空）在激活后于面板内显示原因。
> - 入口按钮只负责激活与加载清单，**绝不自动开始生成**——生成仍需"勾选元素 → 点 `[🧩 生成选中元素图 (K)]`"二次确认（成本安全不变）。
> - 下方"渲染与流程"第 1、2 条的加载时机与门控位置以本块为准；第 4 条警示条耗时文案按 Spike 实测改为"约 1 分钟/张"，并注明"输出为白底 PNG，如需透明请自行二次抠图"（§6.7 定稿）。

**内部状态机**：

```ts
interface ElementItem {           // 从 GET /api/prompts/elements/{ruleId} 加载
  name_cn: string; value_cn: string; position?: string;
  is_text_slot: boolean; extraction_prompt: string;
  checked: boolean;               // 默认全不勾（成本安全，强制显式选择）
  status: 'idle' | 'pending' | 'generating' | 'done' | 'failed';
  taskId?: string; imageUrl?: string;   // done 后：data URI 或 http URL，直接 <img src>
  error?: string;
  downloadChecked: boolean;       // done 后默认 true
}
```

**渲染与流程**：
1. 首次展开时懒加载（`expanded && !loaded` 触发）：并行取元素清单 + `getSupportsReference()`。
2. **门控**（任一命中则整区禁用只显原因文案）：`!supportsReference` → "当前生图接口不支持带参考图（AIReiter 模式），无法拆分元素"；`!ruleCard?.source_images?.length` → "该规则卡没有竞品原图，无法拆分"；元素清单为空 → "未能从规则卡提取到可拆分元素"。
3. 元素清单：checkbox 行（`name_cn` + `value_cn` 摘要 truncate + `position` 小标签 + 文字位显示 `文字位` badge——文字位排最后、默认不勾但**允许勾选**）。
4. 常驻警示条（黄色）："每个元素将单独调用一次生图（约 1~4 分钟/张、按张计费），请按需勾选。生成期间请勿关闭页面。"
5. `[🧩 生成选中元素图 (K)]`（K=0 禁用）→ 勾选项置 `pending` → **串行** for 循环：置 `generating` → `apiPost('/api/gen/submit', ...)` → 成功：`status='done'`、`taskId=tasks[0].task_id`、`imageUrl=tasks[0].image_urls[0]`（OpenAI 同步模式返回即有图；空图会被后端 #17 判 failed 带错误信息）→ 失败：`status='failed'` + error，继续下一个。提供 `[⏹ 停止]`（flag ref，当前张完成后停）。失败项单独 `[重试]`（队列空闲时可点）。
6. 结果区：done 项缩略图网格（img 直接渲染 data URI/URL，点击放大预览可复用 ImageGenSection 现有 previewUrl 模式的简化版）+ 每张自带 downloadChecked 复选（默认勾）+ 单张 `[⬇ 下载]`（调现有 `POST /api/gen/download/{task_id}` → 取 `accessible_paths[0]` → `<a href={getImageUrl(path)} download>` 触发保存）。
7. `[📦 打包下载选中 (M)]`：`fetch(BASE_URL + '/api/gen/download-zip', {method:'POST', headers:{'Content-Type':'application/json'}, body: JSON.stringify({task_ids})})` → `res.blob()` → `URL.createObjectURL` + 临时 `<a download>` 点击 → revoke。错误时读 `res.text()` 展示。
8. 生成期间挂 `beforeunload` 守卫（同阶段二模式）。

#### 6.4.2 生图任务页：`frontend/app/gen/page.tsx`

`VERSION_TABS` 增加 `E: { label: '🧩 元素拆分', short: 'E' }`；`VERSION_ORDER` 改为 `['A', 'B', 'C', 'E', UNVERSIONED]`。（不加也能用——会落"未标记版本"桶，但加上便于运营按用途筛。）

### 6.5 边界与已知限制

- 抠取质量受上游模型能力约束（Spike 已验证的范围内交付）；元素描述过抽象（如"整体氛围"类条目）出图可能发散——V1 接受，用户可不勾此类条目。
- 透明底以 Spike 结论为准，不支持则白底（在 UI 警示条注明"白底 PNG，如需透明请自行抠图"或按结论写）。
- AIReiter 模式整区禁用（`REFERENCE_SUPPORT` 单一事实来源，未来该常量翻转后此功能自动可用，无需改本区代码）。
- 元素图生成不含负向提示词、不拼可定制项（抠取指令自含"remove everything else"）。
- zip 内是任务已下载的本地文件；重复打包幂等（已下载的不重复拉取）。

### 6.6 阶段四验收清单

1. **Spike 报告先行**：2~3 元素 × 白底/透明底实测图 + 三项判定结论，写入本文档与 CLAUDE.md，用户确认 go 后才继续开发。
2. 腊肠犬（或同等）规则卡 → 展开元素拆分区 → 元素清单与规则卡第2/3层一致且无重复条目、文字位在末尾带 badge。
3. 勾 2 个非文字元素生成 → 2 张单元素图逐个串行产出，人工比对保真+干净达标；生图任务页 `E` tab 出现 2 个任务。
4. 勾选 2 张打包下载 → zip 可解压、文件对应正确；单张下载按钮正常。
5. 1 张故意失败（生成中断网）→ 该元素标失败可重试，队列继续。
6. 生图配置切到 aireiter 模式 → 整区禁用显示原因；切回 openai 恢复。
7. 无竞品原图的规则卡（手动构造）→ 整区禁用显示原因。
8. 回归：版本C 原有模板加载/自定义值/生成提示词/生图不受影响；`npm run lint` 通过。

> 【2026-08-17 变更，对应 §6.4.1 变更块】验收清单调整：
> - 第 2 条改为"**点入口按钮后**加载清单再核对"；**新增验收 2b**：打开版本C 后不点入口按钮 → Network 面板确认无任何元素拆分相关请求，界面只有一个入口按钮。
> - 第 6 条：AIReiter 模式下的不支持原因出现在**点击入口按钮后的面板内**（按钮本身可点）。
> - 第 7 条改为：无竞品原图的规则卡 → **入口按钮直接置灰**并提示原因。
> - 第 3 条的"保真达标"判定标准以 §6.7 用户决策块的调优结局档位为准（定位文案对应软化）。

### 6.7 Spike 结论（2026-08-17 实测）

**测试环境**：生图 `api_type=openai`、`model=gpt-image-2`、走 `/v1/images/edits`（带参考图）。
**测试图**：`RULE-0043` 人鱼公主海底梦幻插画（`data/uploads/1784881613_img_v3_0213t_...png`）。选它的理由：5 个非文字可替换元素 + 7 个 must_have，元素分离度高（海龟/水母/珊瑚/鱼群/主体各自清晰），且带完整 `*_en` 平行字段。
**共 5 次真实调用**，单次耗时 36~44 秒（比方案预估的 1~4 分钟快）。

#### 判定表

| 元素 | 变体 | 保真 | 干净 | 透明底 | 输出 |
|---|---|---|---|---|---|
| 海龟（独立物件，易） | 白底 | ⚠️ 偏移 | ✅ 优 | ❌ RGB | 1254×1254 |
| 海龟 | 透明底 | ⚠️ 偏移 | ❌ **反例** | ❌ RGB | 1254×1254 |
| 水母（半透明发光，中） | 白底 | ✅ 好 | ✅ 优 | ❌ RGB | 1024×**1536** |
| 珊瑚礁（一组元素，难） | 白底 | ⚠️ 偏移 | ✅ 优 | ❌ RGB | 1254×1254 |

#### 三项判定标准的结论

1. **干净 ✅ 达标**。白底版三张都只有目标元素，无其他元素/文字/背景残留。客观检查：四角 RGB 范围 `[253,253,253]~[255,255,255]`（接近纯白，可用工具二次抠成透明），纯白像素占比 64%~85%。**这一项超出预期**。

2. **保真 ⚠️ 部分达标，是本次最大的发现**。同一套指令在不同元素上表现不一致：
   - **水母保真好**：配色（粉紫渐变）、发光质感、触须形态都与原图一致。
   - **海龟与珊瑚礁有风格偏移**：抠出来更偏**写实/3D 渲染**质感，丢了原图的手绘插画笔触。海龟从"插画里的海龟"变成了"照片级海龟"；珊瑚礁变成了排列整齐的 3D 珊瑚墙，原图里那种散落在画面两侧的松散感消失了。
   - **推测原因**（未进一步验证）：① 测试图本身是**装裱画布的 mockup**（有画框立体边缘），模型可能把它当成实物照片而非平面印花；② `Reproduce it exactly as it appears` 这类指令对"风格"的约束力弱于对"内容"的约束力——模型知道要画什么，但不严格复刻怎么画。
   - 这不是"重新想象的同类物"（不是换了一只别的海龟），构图与配色仍对应原图，属于**风格漂移而非内容失真**。

3. **透明底 ❌ 不支持，且请求它会产生反例**。`on a fully transparent background` + `Output PNG with alpha transparency` 的结果：
   - 返回的图是 **RGB 模式、无 alpha 通道**（Pillow 检查确认）；
   - 更糟的是模型把"透明背景"理解成了**要画出棋盘格图案**——那些灰白格子是**实际像素**，不是透明（纯白占比只有 34.4%，其余是格子的灰色）。这比白底更难处理，因为要二次去掉的是有图案的背景。
   - **结论：绝不能给用户"透明底"选项**，只交付白底。

#### 对实现的影响（定稿）

- **指令模板定稿用白底版**，`ELEMENT_EXTRACTION_PROMPT_TEMPLATE` 取 §6.2 的白底文本，**不提供透明底选项**。
- UI 警示条须注明：**输出为白底 PNG，如需透明请自行二次抠图**（白底纯净度 253-255，主流抠图工具处理无压力）。
- **须向用户明示"风格可能偏移"**：抠出的元素在配色/构图上对应原图，但笔触风格可能偏写实。适合当"素材参考/拼贴底图"，不适合当"必须与原图风格完全一致的贴图"。
- **输出画布比例不受控**：请求 1024×1024，水母那次返回 1024×1536——模型按元素形状自行调整。前端缩略图网格必须用 `object-contain` 或允许不等高，不能假设方图。
- 耗时按 **40 秒/元素**估算（比原预估乐观），5 个元素约 3.5 分钟。

#### go / no-go 建议

**建议 go，但需下调对"保真"的承诺**：干净度与链路完全达标、耗时可接受；保真是"内容对、风格可能漂"。功能定位应从"精确的元素分离素材"调整为"**基于原图元素的素材图生成**"。

若你认为"风格必须严格复刻"是硬需求，则应判 **no-go 或先做指令调优**（可尝试在 prompt 里显式锁定风格词，如 `keep the exact same hand-painted illustration style, do not make it photorealistic`——但这需要额外几次调用验证，未包含在本次 Spike 内）。

**决策权在你**，我不擅自推进 §6.3 之后的开发。

（Spike 产出的 4 张实测图在 `/tmp/spike/`：`turtle_white.png`、`turtle_transparent.png`、`jellyfish_white.png`、`coral_white.png`）

#### 用户决策（2026-08-17）：先做指令调优轮再定位，无论结局均 go

追加一轮小规模验证（算 Spike 延伸，结论继续追加在本节之后，工单只追加）：

1. 只重测两个失败元素（海龟、珊瑚礁），每个测 2 个指令变体，共 ≈4 次调用：
   - **变体 A（风格锁定）**：白底版模板 + `Preserve the exact same artistic style as the original — hand-painted illustration look, same brush strokes, flat shading and colors. Do NOT render it photorealistic or 3D.`
   - **变体 B（风格锁定 + 消除 mockup 干扰）**：变体 A + `The provided image is a flat 2D printed artwork; treat it as digital illustration, ignore any frame, canvas texture, or physical product context.`
2. 判定标准与首轮相同（保真/干净），对照水母首轮结果。
3. 三档结局（**均为 go**，按结果定档）：
   - 海龟+珊瑚都到"好" → 定稿含风格锁定的模板，功能定位保持"元素分离素材图"；
   - 有改善但不稳定 → 定稿最优模板，UI 文案软化："白底 PNG，风格可能有偏移，如需透明请自行抠图"；
   - 无改善 → 功能定位降级为"**基于原图元素的素材图生成**"（UI 文案同上）。
4. 无论哪档，以下按首轮结论**无条件落实**：永不提供透明底选项；UI 明示"白底 PNG（底色 RGB 253~255，可二次抠透明）"；结果网格 `object-contain` 自适应比例、不假设方图；警示条耗时文案"约 1 分钟/张"。

> **⚠️ 本条第 1、2 项已于 2026-08-17 被验收后的复测推翻，以 §6.9 变更记录（二）为准**：
> - "永不提供透明底选项" → **改为默认就出透明底**（首轮漏试了 API 的 `background` 参数；现方案是"抠白底 → 后端 `white_to_transparent()` 转真透明"，实测白边残留 0%）。仍然成立的部分：**不要在 prompt 文字里要求 transparent**，那样模型会去画棋盘格图案。
> - "UI 明示白底 PNG" → 改为"透明底 PNG，保持原图中的位置与比例"。
> - 第 3、4 项（`object-contain` 不假设方图、耗时"约 1 分钟/张"）继续有效。

> **⚠️ 2026-08-18 再次修正：上游返回的到底是不是白底图，此前一直判断错了**。
> 详见 §6.9 变更记录（三）。要点：不传 `background` 参数时上游**自作主张返回
> RGBA 已抠图**（不是白底 RGB），且它抠得有碎洞——用户反馈的"主体元素缺失"根因
> 在此，不在本地阈值算法。现改为显式 `background="opaque"` 要整图、本地自己抠。

#### 调优轮实测结论（2026-08-17，Spike 延伸，4 次真实调用）

**测试条件**：同首轮（`RULE-0043` 人鱼公主图、openai/gpt-image-2、`/v1/images/edits`）。只重测首轮失败的海龟 + 珊瑚礁，各测变体 A/B。耗时 31~48 秒/次。

| 元素 | 首轮（无风格锁定） | 变体 A（风格锁定） | 变体 B（A + 消除 mockup） |
|---|---|---|---|
| 海龟 | ❌ 照片级写实，丢笔触 | ✅ 明显改善：甲壳有手绘描边、青绿高光回来 | ✅ **最佳**：蓝绿虹彩高光 + 插画描边，写实感进一步减弱 |
| 珊瑚礁 | ❌ 排列整齐的 3D 珊瑚墙，散落感消失 | ✅ 明显改善：水彩笔触、保留星光颗粒、V 形分布近似原图 | ✅ **最佳**：水彩感最强，**连原图的气泡都还原了** |

**客观数据**（背景纯净度不受风格锁定影响，三轮一致）：

| 文件 | 尺寸 | 纯白占比 | 角落 RGB 下限 |
|---|---|---|---|
| turtle_white（首轮） | 1254×1254 | 64.1% | 253 |
| turtle_tuneA | 1308×1203 | 63.5% | 253 |
| turtle_tuneB | 1344×1170 | 59.8% | 253 |
| coral_white（首轮） | 1254×1254 | 65.4% | 253 |
| coral_tuneA | 1254×1254 | 57.9% | 253 |
| coral_tuneB | 1254×1254 | 57.9% | 253 |

**结论**：
1. **风格锁定句有效且必要**（变体 A 相对首轮就有明显改善）——首轮"风格漂移"的根因确认为 `Reproduce it exactly as it appears` 对"怎么画"约束不足，补一句显式风格约束即可大幅缓解。
2. **消除 mockup 干扰句进一步有效**（变体 B > 变体 A）——印证首轮的推测②：测试图是装裱画布 mockup，模型会把画框/画布纹理当成实物语境；明确告知"这是平面 2D 印刷图案"后写实倾向再降一档。
3. **干净度不受影响**：三轮角落 RGB 下限恒为 253，纯白占比 58%~65% 的差异只是元素占画面比例不同（B 的元素画得更满），不是背景变脏。
4. **画布比例仍不受控**（海龟 B 是 1344×1170），确认首轮结论，前端必须 `object-contain`。

**定档：落在第一档（海龟+珊瑚都到"好"）→ 定稿含风格锁定的模板，功能定位保持"元素分离素材图"。**

**`ELEMENT_EXTRACTION_PROMPT_TEMPLATE` 定稿文本（= 变体 B）**：
```
Extract only {element} from the provided image. Reproduce it exactly as it appears in
the image — same style, same colors, same details. Place it alone, centered, on a plain
pure white background. Remove all other elements, all text, and the original background.
Preserve the exact same artistic style as the original — hand-painted illustration look,
same brush strokes, flat shading and colors. Do NOT render it photorealistic or 3D.
The provided image is a flat 2D printed artwork; treat it as digital illustration, ignore
any frame, canvas texture, or physical product context.
```

**UI 定位文案（第一档）**：功能名保持"元素拆分图"，警示条写"每个元素单独调用一次生图（**约 1 分钟/张**、按张计费），输出为**白底 PNG**（底色 RGB 253~255，如需透明请自行二次抠图）。生成期间请勿关闭页面。"——**不加"风格可能偏移"的软化文案**（第二/三档才需要）。

**保留的已知限制**（写入 §6.5）：调优仅在一张图（装裱画布 mockup 类）上验证过；不同风格的竞品图（如纯文字排版、照片拼贴类）效果未测，实际使用中若遇风格漂移，调 `ELEMENT_EXTRACTION_PROMPT_TEMPLATE` 常量即可（已常量化，便于调优）。

（调优产出 4 张实测图：`/tmp/spike/turtle_tuneA.png`、`turtle_tuneB.png`、`coral_tuneA.png`、`coral_tuneB.png`）

### 6.8 阶段四收尾动作

- CLAUDE.md 增补：元素拆分链路（元素收集去重规则、extraction prompt 模板位置、version='E' 约定、zip 端点为裸二进制响应例外）。
- 本文档 §9 工单追加。

---

## 7. 全局风险与回归关注点

| 风险 | 涉及阶段 | 缓解 |
|---|---|---|
| AI 多套输出截断（MAX_TOKENS=8192） | 三 | §5.2.4 实测步骤；截断则上限降 3 并记录 |
| 多套方案同质化 | 三 | prompt 显式差异化要求 + 验收人工比对；仍同质则在 prompt 中追加"每套指定不同主攻维度"的约束（变更记录进本文档） |
| 元素抠取保真度不足 | 四 | Spike 先行 go/no-go；模板常量化便于调优 |
| 批量分析长时间占用（N×1分钟） | 二 | 串行+可停止+每张完成即收起可见进度；上限 20 张 |
| 生图费用放大（多套/多元素） | 三、四 | 默认不勾选（元素）、明示计费与耗时警示、批量上限 |
| prefs 并发写（多标签页） | 一 | 后端锁保证文件不损坏；last-write-wins 可接受（已记录） |
| `PromptDisplay` key 重挂载丢编辑 | 三 | UI 明示"切换方案会重置手动编辑"；批量栏用原始提示词并明示 |

**每阶段固定回归项**：单图分析→保存→A/B/C 生成→生图全链路手动跑一遍；`npm run lint`。

---

## 8. 与三层文档的对账关系

- **宪法（CLAUDE.md）**：每阶段收尾把"关键架构约定"增补进对应章节（新增"三期"小节或并入现有分区），与本文档不冲突。
- **方案（本文档）**：实施中所有偏离本文档的决定，当场以"变更记录"追加（日期+理由），不回改原文。
- **工单（§9）**：只追加不回改。

## 9. 工单（任务单，只追加不回改）

> 实施者在每项完成时追加一行 `- [x] YYYY-MM-DD 完成说明`；变更/阻塞也在此追加，不修改历史行。

### 阶段一：偏好持久化（需求2）
- [ ] 后端 `services/user_prefs_store.py`（锁+shape校验+原子写）
- [ ] 后端 `routers/user_prefs.py` 六个端点 + main.py 注册
- [ ] 前端 `lib/sizePresets.ts` 抽取（纯搬移）
- [ ] 前端 `lib/userPrefs.ts`（模块缓存+静默降级）
- [ ] `ProductSelect.tsx` 合并已保存产品 + chips 删除
- [ ] 三个版本组件 handleGenerate 加 `addCustomProductIfNew`
- [ ] `ImageGenSection` 自定义尺寸预设 + last_size 恢复/记忆 + 保存/删除 UI
- [ ] §3.5 验收清单全过 + lint
- [ ] CLAUDE.md 对账 + 等用户验收确认

#### 阶段一完成记录（追加）

- [x] 2026-08-17 后端 `services/user_prefs_store.py` 完成：模块级 `_LOCK` + `atomic_write_json`；`load_prefs` 逐字段 shape 校验（`_sanitize_products`/`_sanitize_size_presets`/`_sanitize_last_size`），损坏静默丢弃不抛错；`add/remove_custom_product`、`add/remove_custom_size`、`set_last_size` 按 §3.2.1 设计实现（去重最新在前、20 条上限、长度与 1~10000 尺寸校验抛 ValueError）。
- [x] 2026-08-17 后端 `routers/user_prefs.py` 六个端点完成 + `main.py` 注册；统一 `{"success": True, "data": ...}` 包装；ValueError→400、其余→`logging.exception`+500。
- [x] 2026-08-17 后端自测（TestClient，10 组边界）全过：默认空值 / 去重最新在前 / 空名超长 400 / 幂等删除 / 21 条截断为 20 / 尺寸同名更新 / 越界 400 / 手动模式空 preset / 文件损坏回落默认值并可重建 / 部分字段损坏只丢该字段 / 顶层非 dict 不炸。
- [x] 2026-08-17 真实 HTTP 链路验证：CORS 预检 200、`access-control-allow-origin: http://localhost:3000` 正确；中文+空格产品名经 `encodeURIComponent` 的 DELETE 正常。
- [x] 2026-08-17 前端 `lib/sizePresets.ts` 抽取完成（纯搬移 `PRODUCT_SIZE_PRESETS`/`API_MAX_SIZE`，另导出 `CUSTOM_SIZE_PREFIX`；`gcd`/`calcRatioText` 按方案留在 `PromptDisplay.tsx`）。
- [x] 2026-08-17 前端 `lib/userPrefs.ts` 完成（`_cache` + `_inflight` 并发去重、fire-and-forget 与 await 两类写语义、失败静默降级不写缓存）。
- [x] 2026-08-17 `ProductSelect.tsx` 改造完成（挂载读偏好、下拉合并 `✏️ ` 前缀已保存项并与规则卡选项去重、自定义模式 chips 点选/删除）；A/B/C 三个版本组件 `handleGenerate` 各加一行 `addCustomProductIfNew`（不 await）。
- [x] 2026-08-17 `ImageGenSection` 改造完成（自定义预设合并进下拉、`touchedRef` 守卫下的 last_size 恢复、500ms debounce 记忆且新值传参不读闭包、手动态"💾 保存为常用尺寸"内联表单、选中自定义预设时"🗑"删除）。
- [x] 2026-08-17 §3.5 验收清单全过（Playwright 真实浏览器操作，共 43 项断言）：
  - 产品持久化 9/9：自定义输入→生成→落后端；硬刷新后 A/B/C 下拉均出现 `✏️ Pillow 抱枕`；选中可用；chips 删除后端同步；刷新后彻底消失；全程无 JS 错误。（清单 1、2）
  - 尺寸持久化 19/19：手动 3000×3000 → last_size 落盘、预设退回手动态；保存为预设"抱枕 45x45" → 刷新后下拉出现 `[自定义] 抱枕 45x45 (3000×3000)` 且选中态与宽高都恢复；内置预设联动 4000×6000 并记住；删除预设后下拉消失、宽高保留、退回手动态；last_size 指向已删预设时刷新退回手动模式 + 原宽高不报错。（清单 3、4、5）
  - 降级 7/7：拦截 `/api/prefs**` 全部失败 → 下拉照常渲染内置项、生图区回落 square、内置 17 预设完整、无 JS 错误。（清单 6 的前端侧；后端侧由自测 [8]/[9]/[10] 覆盖）
  - 回归 8/8：生图提交请求体与改造前一致（4000×6000 → 1067×1600 等比缩放、version/count/attach_rule_image/reference_image_paths 字段齐全）。（清单 8）
  - 清单 7（21 条截断）由后端自测覆盖。
- [x] 2026-08-17 `npm run lint` 与基线一致（改动前后同为 19 errors / 23 warnings，`git stash` 对比确认**本次未新增任何 lint 问题**；既有 error 为历史遗留的 `no-explicit-any` 与 `react-hooks/set-state-in-effect`，不在本阶段范围）；`npx tsc --noEmit` 零错误。
- [x] 2026-08-17 CLAUDE.md 对账完成：后端章节增补"用户偏好存储（三期阶段一）"；前端章节增补"尺寸预设单一事实来源""用户偏好前端层""生图尺寸恢复的 touchedRef 守卫"三条。
- [x] 2026-08-17 **本阶段实施未偏离方案文档**，无变更记录需追加。测试数据已清理（`data/user_prefs.json` 还原为不存在状态），未产生多余生图任务。


### 阶段二：批量上传分析（需求4）
- [ ] `FileUpload.tsx` multiple 支持（向后兼容，grep 全部引用确认）
- [ ] `analyze/page.tsx` batch 状态机 + 串行队列 + 自动保存
- [ ] 完成条目收起紧凑行 + 分析中展开卡 + 计时
- [ ] 停止/重试/beforeunload/20 张上限
- [ ] §4.4 验收清单全过（含单图流程回归）+ lint
- [ ] CLAUDE.md 对账 + 等用户验收确认

#### 阶段二完成记录（追加）

- [x] 2026-08-17 `FileUpload.tsx` 多选支持完成：新增可选 `multiple` + `onFilesSelect`（收集全部 `image/*`，只选 1 张也包装成数组交父组件分流）；不传 `multiple` 时行为与改造前完全一致。grep 确认仅两处引用——`analyze/page.tsx`（改为 multiple）与 `PromptDisplay.tsx` 的"添加参考图"（保持单文件，未受影响）。multiple 模式下不在组件内预览（父组件网格展示），change 后清空 `input.value`（否则再选同一批文件不触发事件）。
- [x] 2026-08-17 `analyze/page.tsx` batch 状态机 + 串行队列 + 自动保存完成。入口分流 `handleFilesSelect`：1 张→原单图全流程；≥2 张→批量（上限 20 张，超出截断+提示）。
- [x] 2026-08-17 完成条目收起紧凑行（缩略图+文件名→规则名+SABC badge+去编辑新窗）/ 分析中展开卡（大图+spinner+per-item 计时）/ 排队中置灰行 / 顶部进度计数 / done 汇总条 + 查看规则库链接，全部完成。
- [x] 2026-08-17 停止队列（当前张跑完后停、剩余保持 queued、按钮变"停止中"、可点"继续"）/ 单张重试（置回 queued 复用同一循环）/ beforeunload 守卫 / 20 张上限，全部完成。
- [x] 2026-08-17 **实现细节偏离预案的技术决策（方案 §4.2.2 未预见，当场记录）**：原计划用 `setBatchItems(prev => …)` 函数式更新维护队列，实施中发现两个问题——① 执行循环是跑几十分钟的 async 函数，闭包读 state 永远是启动那刻的旧值；② 若用"在 setState 更新函数里 resolve promise 取当前值"绕过，该更新函数不纯，在 **React StrictMode（Next.js dev 默认开启）下会被双调用**，取到错乱值。**改为：权威队列放 `batchItemsRef`，`batchItems` state 仅作渲染镜像，统一经 `writeBatchItems`/`patchBatchItem` 同时写两者。** 理由：ref 读写同步且不受 StrictMode 双调用影响，是长跑循环访问可变队列的唯一可靠方式。已同步记入 CLAUDE.md。
- [x] 2026-08-17 **另一处偏离：per-item 计时器与"停止中"按钮态的实现方式（受 ESLint 规则约束）**。项目 ESLint 启用了 `react-hooks/set-state-in-effect`（禁 effect 体内同步 setState）与 `Cannot access refs during render`（禁渲染期读 ref）。故计时器改为"事件上下文里 `setItemStartAt(Date.now())`，effect 只负责起 interval"；"停止队列"按钮的禁用态用 `stopping` state 而非读 `stopFlagRef.current`。已同步记入 CLAUDE.md。
- [x] 2026-08-17 §4.4 验收清单全过（Playwright 真实浏览器，共 45 项断言）：
  - 队列机制 20/21（mock 接口，不烧 AI 额度）：4 张进批量 ready 态、缩略图网格、全局 hint 输入框；**严格串行实测 max_concurrent=1**；4 次 analyze 每次都带上 hint；成功 3 张自动保存且带 `thumbnail_path`；完成即收起紧凑行（3 个"去编辑"）；第 3 张模拟失败标记原因+重试按钮、队列继续未阻塞；重试后成功 4 张。（清单 1、4）
    - 唯一未通过项是**测试脚本自身的问题**（mock 的 `json.dumps` 未加 `ensure_ascii=False`，把中文错误信息转成了 `模拟…` 转义串，故断言"页面含中文错误原因"不成立）；截图 `/tmp/p2_done.png` 确认失败行的错误原因与重试按钮均正确渲染，功能无缺陷。
  - 停止/守卫/超选/单图回归 16/16：中途停止→只跑了 2 张、剩余 2 张保持排队、"继续"后跑完 4 张；运行中导航离开→实测触发 `beforeunload` 确认框；超选 25 张→提示"单批最多 20 张，已保留前 20 张（其余 5 张请下一批再传）"且计数为 20；**只选 1 张→进 ready 态而非批量、未自动调 analyze、分析后进人工预览未自动保存、手动保存后原地展开提示词、全程只保存 1 次**。（清单 3、5、6、7）
  - 真实链路 8/8（真实 AI + 真实入库）：2 张图带 hint"重点关注宠物纪念送礼场景"→串行分析→**规则库真实新增 RULE-0059/RULE-0060**，缩略图正常、SABC 等级正常（均 B 级）、6 层齐全（`layer_0_core`/`layer_1_commercial`/`layer_2_visual`/`layer_3_variable`/`layer_4_product`/`layer_5_data`）、hint 明显生效（核心卖点含"适合送礼和长期留念"）。（清单 2）
  - `FileUpload` 向后兼容 3/4：参考图区 input **未开 multiple**、原有"用途输入+AI分析"流程正常、无 JS 错误；未通过项同为测试脚本问题（purpose 提示语在 input 的 placeholder 属性里，不在 body 文本中），截图 `/tmp/p2_refupload.png` 确认功能正常。
  - 全部场景 **零 JS 运行时错误**。
- [x] 2026-08-17 `npm run lint` 回到基线（19 errors / 23 warnings，与改动前完全一致——实施中曾引入 2 个新 error（effect 内 setState、渲染期读 ref），已按上面第二条偏离记录修正）；`npx tsc --noEmit` 零错误。
- [x] 2026-08-17 CLAUDE.md 对账完成：前端章节增补"批量分析队列（三期阶段二）"（含串行理由、ref 权威队列、循环语义、ESLint 约束四个子条）与"`FileUpload` 多选模式"（含两处调用方必须同时验证的提醒）。
- [x] 2026-08-17 **遗留待用户处置**：真实链路测试产生的 RULE-0059 / RULE-0060 两张规则卡仍在规则库中（验收时可用于查看批量结果，确认后可自行删除）。

##### 阶段二变更记录 · 2026-08-17（用户验收反馈）

**反馈**：① 上传一张后无法再次添加图片；② 单批上限从 20 改为 10。

**变更 1：上限 20 → 10**（`BATCH_MAX = 10`）。理由：用户要求；每张约 1 分钟，10 张已是 10 分钟量级。方案 §4.2.2「上限 20 张」、§4.3、§7 风险表「上限 20 张」相关表述以本条为准。

**变更 2：补齐"继续添加图片"入口（方案 §4.2.2 未设计，属功能缺口）**。原实现只在 `idle` 态渲染上传区，一旦进入 ready/batch 态就没有再次添加的入口，想多传一张必须整批重选。新增三处入口 + 一个移除能力：

| 位置 | 行为 |
|---|---|
| 批量 `ready` 态 | `handleAppendFiles` 追加进现有队列（不清空已有），显示"还可添加 N 张"；达上限时改显示"已达单批上限"并隐藏上传框 |
| 批量 `running`/`done` 态 | 同上。**运行中追加会自动排到队尾被跑掉**——执行循环每轮都重新从 `batchItemsRef` 找 `queued` 项，天然支持；已停止/已完成时追加后点"继续"即可 |
| 单图 `ready` 态 | `handleAddFromReady` 把"已选的那 1 张 + 新加的"一起转为批量模式（`buildBatch`），不用为多传一张而重选 |
| 批量 `ready` 态缩略图 | 每张右上角 `✕` 移除（`handleRemoveItem`，只允许移除 `queued` 项并 revoke 其 Object URL）——有了追加就必须能删，否则误加一张只能整批重来 |

实现要点（已同步 CLAUDE.md）：
- 抽出 `buildBatch(files)` 作为"≥2 张构建队列"的公共入口，供"一次选多张"与"单图态追加转批量"共用。
- **追加项的 id 用"现有最大 id + 1"生成，不能用数组长度**——移除中间某项后长度会与仍存在的 id 撞车（与后端递增 ID 的 scan-max 教训同源）。
- 追加时按剩余额度 `room = BATCH_MAX - 现有张数` 裁剪，超量部分提示"本次只添加了 N 张"；`room <= 0` 时拒绝并提示。
- `handleAddFromReady` 先 revoke 单图态的 `imagePreview` 再交给 `buildBatch` 重建（批量态会为每个文件新建 Object URL），避免泄漏。

**验证**（Playwright 真实浏览器，23/23 通过）：ready 态追加 2 张→共 4 张且原有未被清空、剩余额度实时更新；移除误加项→计数与缩略图同步；**运行中追加 1 张→自动排队尾并跑完（4 次分析），且串行未被破坏（max_concurrent=1）**；单图态追加 2 张→转批量共 3 张且原图仍在；超选 25 张→提示"单批最多 10 张，已保留前 10 张（其余 15 张请下一批再传）"；达上限后移除 1 张再加 3 张→只添加 1 张并提示其余顺延。全程零 JS 错误；`lint` 仍为基线 19/23，`tsc` 零错误。


### 阶段三：版本B 多方案（需求3）

#### 阶段三·前置：规则库批量删除（追加需求5，2026-08-17 用户提出）
- [ ] 后端 `POST /api/rules/batch-delete`（复用 `delete_rule`、串行、部分失败不中断、上限 100）
- [ ] `RuleCardList.tsx` 选择模式（勾选框/全选当前筛选结果/反选/批量删除/结果提示）
- [ ] 选择模式下不渲染 Link（点卡片=勾选）、隐藏单张 🗑
- [ ] §5.0.5 验收清单全过 + lint
- [ ] CLAUDE.md 对账（§5.0.6）

##### 阶段三·前置 完成记录（追加）

- [x] 2026-08-17 后端 `POST /api/rules/batch-delete` 完成（`routers/rules.py`）：`BatchDeleteRequest` pydantic 模型；空列表/超 100 条 → 400；串行复用 `delete_rule()` 不重写删除逻辑；单条异常 `logging.exception` 后计入 `failed` 继续下一条；响应 `{deleted, not_found, failed}` 三分类 + 包装格式。路由声明顺序在 `PUT/DELETE /rules/{rule_id}` 之前（避免 `batch-delete` 被当成 rule_id 匹配）。
- [x] 2026-08-17 后端自测 23/23 通过：参数校验（空/超限 400）；真实建 3 张带独立图片文件的卡 → 批量删 2 张 + 1 个不存在 ID → `deleted`/`not_found`/`failed` 分类正确；**逐条核对 JSON 文件、SQLite 行、`data/uploads/` 关联图片三处全部清理**；未删的第 3 张三处完好；幂等重删计入 `not_found` 不报错；列表接口未受影响。
- [x] 2026-08-17 `RuleCardList.tsx` 选择模式完成：工具栏「☑️ 批量删除」入口（列表为空不显示）→ 操作条（已选计数 + 当前筛选条数 + `全选当前 N 条`/`取消全选` + `反选` + `🗑 删除所选 (K)` danger 按钮 + `取消`）；卡片左上角勾选框（样式复用版本A 推荐图网格）+ 选中高亮边框；二次确认含条数与"不可恢复、同时删除关联竞品原图"；结果提示条可 × 关闭。
- [x] 2026-08-17 选择模式下不渲染 `<Link>` 改渲染 `role="checkbox"` 的 `<div>`（含 `aria-checked`/`tabIndex`/空格回车键盘支持），单张 `🗑` 隐藏。
- [x] 2026-08-17 §5.0.5 验收（Playwright 真实浏览器 + 真实后端删除，共 60 项断言）：
  - 首轮 27/28：选择模式进入/退出、链接消失与恢复、单张 🗑 隐藏与恢复、K=0 禁用、勾 3 条删除后列表与后端总数同步（65→62）、刷新后确认落地、**C 级筛选下"全选当前 4 条"不扩散到其他等级（切回全部仍只选中那 4 条）**、反选两次回到原选、`取消` 清空勾选、混合场景分条提示、四张测试卡三处文件全清理、原有搜索/排序/筛选回归。
  - **实测发现 1 个真实缺陷并修复**：`not_found` 项（后端已不存在）原实现未从列表移除，成为幽灵卡片（点进去只会 404）。修复为 `[...deleted, ...notFound]` 都移除，只保留 `failed`（卡还在、可重试）。理由已记入 CLAUDE.md。
  - 修复后复验 5/5：3 张卡其中 1 张先被别处删掉 → 提示"已删除 2 条，⚠️ 1 条已不存在" + **列表 3 张全部移除无残留** + 后端总数 63→60 + 无 JS 错误。
  - 全程零 JS 运行时错误；测试数据（规则卡 + uploads 图片）已全部清理，规则库回到 60 张。
- [x] 2026-08-17 `npm run lint` 与基线一致（19 errors / 23 warnings）；`npx tsc --noEmit` 零错误。
- [x] 2026-08-17 CLAUDE.md 对账完成（§5.0.6）：后端章节增补"规则卡批量删除"（POST 而非 DELETE-with-body 的理由、复用 `delete_rule`、三分类响应、无需加锁的理由）；前端章节增补"规则库选择模式"三条约定（全选范围只作用于筛选结果、不渲染 Link 的可访问性理由、`deleted`+`not_found` 都移除只留 `failed`）。

#### 阶段三·主体：版本B 多方案
- [ ] `get_recommendation_prompt` num_directions 分支（占位符铁律）
- [ ] `_assemble_version_b_direction` 抽取（N=1 行为等价）
- [ ] `_ai_recommend_multi` + `_random_recommend_multi`（轮转降级）
- [ ] `generate_version_b` 多方向返回 `{directions}` / N=1 旧形状
- [ ] 路由 `num_directions` 参数（ge=1 le=4）
- [ ] N=4 截断实测（§5.2.4）
- [ ] 前端 `lib/genConfig.ts` 提取 supports_reference 缓存
- [ ] `PromptVersionB.tsx` 方案数选择 + 方案卡片 + key 重挂载 PromptDisplay
- [ ] 批量生成栏（尺寸/附原图开关/串行提交/状态行/范围限制文案）
- [ ] §5.4 验收清单全过 + lint
- [ ] CLAUDE.md 对账 + 等用户验收确认

##### 阶段三·主体 完成记录（追加）

- [x] 2026-08-17 `get_recommendation_prompt` 加 `num_directions` 参数（默认 1）：N=1 直接 return 原文本、**实测与改造前逐字符一致**（用 `git show HEAD` 取旧实现对比）；N>1 走新的 `_get_multi_recommendation_prompt`，保留原 6 条要求 + 新增第 7 条差异化硬要求（"不允许多套方案只是在同一个维度上换近义词"）+ 第 8 条顶层 slots 要求；JSON 示例全部方括号占位符，无任何看起来像答案的具体值（铁律 §2-6）。
- [x] 2026-08-17 `_assemble_version_b_direction` 抽取完成（纯重构）：单套/多套共用同一份组装逻辑。**等价性实测：6 张真实规则卡 × 3 种目标产品 = 18 组，改造前后逐字段完全一致**（无 AI 模式确定性对比）。
- [x] 2026-08-17 `_ai_recommend_multi`（一次调用、不传 temperature 保持创意多样性、逐项过滤 `recommended_changes` 非空 dict、有效项不足记 warning 照常返回）+ `_random_recommend_multi`（第 i 套取 `alternatives[i % len]`，空 alternatives 回落 original，降级原因写进 reason）完成。
- [x] 2026-08-17 `generate_version_b` 多方向返回 `{directions, num_directions}` / N=1 旧扁平形状；`adaptation` 与 `customization_slots` 各算一次多套共用。
- [x] 2026-08-17 路由 `GenerateBRequest.num_directions: int = Field(default=1, ge=1, le=4)` + 透传；`ai_mode` 与响应包装不变。
- [x] 2026-08-17 **§5.2.4 N=4 截断实测通过**：真实 AI（gpt-5.4-mini）N=4 返回 4 套完整方案、耗时 28.2s、最后一套 `recommended_changes`/`reason`/`image_prompt_positive`/`image_prompt_negative` 全部完整闭合、无降级、4 套组合各不相同。**无需下调上限，`le=4` 保持**。（N=1 约 16s、N=3 约 22s）
- [x] 2026-08-17 前端 `lib/genConfig.ts` 提取完成（`getSupportsReference`/`getCachedSupportsReference`，模块缓存+并发去重，失败保守 false 且不写缓存）；`PromptDisplay.tsx` 改为 import 使用，行为不变。顺带把 `clampToApiMax`/`buildSizeOptions`/`resolveSizeByValue` 抽到 `lib/sizePresets.ts` 供生图区与批量生成栏共用，`PromptDisplay` 里原地重复的三段逻辑改为调用它们。
- [x] 2026-08-17 `PromptVersionB.tsx` 完成：方案数按钮组（1/2/3/4，默认 3）+ 响应形状分流 + 方案卡片列表（勾选框/标题/正在查看标记/reason 摘要 line-clamp-2/改动概览前 3 条+"等 X 处改动"）+ `key={activeIdx}` 重挂载 PromptDisplay + "切换方案会重置手动编辑"提示。
- [x] 2026-08-17 `BatchGenerateSection` 批量生成栏完成：勾选计数、尺寸下拉（复用 `buildSizeOptions` + `fetchPrefs` 的 last_size 默认值）、附带竞品原图开关（三种置灰原因镜像 PromptDisplay）、串行提交、每套状态行、完成提示+生图任务页链接、范围限制明示文案。
- [x] 2026-08-17 **实施中的技术决策（方案 §5.3.2 未预见，当场记录）**：批量生成栏"附带竞品原图"开关的默认值原计划用 effect 同步 state（镜像 PromptDisplay 的 `userToggledRef` 做法），但项目 ESLint 的 `react-hooks/set-state-in-effect` 会报错（阶段二同一条规则踩过）。**改为派生值 `userRefChoice ?? (hasRuleImage && supportsReference && allHaveEdit)`**——`userRefChoice: boolean | null`，null 表示用户还没手动切过。好处不只是过 lint：`supportsReference` 是异步拉回来的，派生值天然能在它到达后自动结算，不需要额外的"重新结算 effect"。已同步记入 CLAUDE.md。
- [x] 2026-08-17 §5.4 验收清单全过（后端 Python 直测 + Playwright 真实浏览器，共 84 项断言）：
  - 后端真实 AI 19/19（清单 1、2、5）：不传参数 → 旧单套形状；N=3 → 3 套形状/字段与单套逐字段一致、**3 套 recommended_changes 各不同且 reason 各写明不同"主攻方向"**（婴童礼物 / 送妈妈朋友 / 家居装饰轻童话）、英文提示词与编辑指令各 3 种、slots 顶层一份、非降级；N=4 无截断；`num_directions=0/5` → 422。
  - N=1 等价性 14/14（清单 1）：6 卡 × 3 产品 = 18 组逐字段一致；prompt 文本逐字符一致。
  - 前端 UI 43/44 + 复验 13/14（清单 2、3、4）：默认选中 3；N=1 无方案卡片/无批量栏、生图提示词与可定制项与生图区都在（与改造前一致）；N=3 出 3 张卡片且**内容各不相同**、卡片改动维度与展开的「🎯 AI 推荐改动」一致；切换方案 → 生图提示词变化、"正在查看"标记跟随、**三栏布局未破坏、版本C 同屏可操作**；批量栏展开显示"已勾选 3 / 3 套"、串行提交 3 个任务、3 个状态行显示已提交、3 套提示词各不相同、`version=B`/`count=1`/`attach_rule_image=true`/编辑指令式提示词/尺寸已缩放；取消勾选 1 套 → 计数与实际提交数同步为 2；**点勾选框不触发方案切换**。
    - 两轮共 2 项未通过均为**测试脚本自身问题**，非代码缺陷：① 首轮用 `time.sleep()` 阻塞了 Playwright 事件循环、饿死 route mock handler，导致只捕获到 2/3 次提交（改用 `page.wait_for_timeout` 后 3/3 全捕获）；② "3 个状态行"断言用 `body.count("✅ 已提交")` 把完成提示条"✅ 已提交 3 个任务"也算进去得 4（截图 `/tmp/s3b_batch.png` 确认恰好 3 行）。
  - 降级 16/16（清单 6）：AI 调用抛异常 / 返回非 JSON / 返回 JSON 但结构不对 → 均返回 3 套轮转方案且**各套不同**、reason 注明降级原因、可定制项走规则卡兜底；AI 只返回 2 套有效 → 照常返回 2 套不报错且保留 AI 的 reason、AI 显式空 slots 被尊重不兜底；无 AI 客户端 → 轮转且 reason 不含"降级"。
  - 全程零 JS 运行时错误；生图提交全程 mock，**未产生任何真实生图任务/费用**。
- [x] 2026-08-17 `npm run lint` 与基线一致（19 errors / 23 warnings，实施中曾引入 1 个 `set-state-in-effect` error，已按上面的派生值方案修正）；`npx tsc --noEmit` 零错误。
- [x] 2026-08-17 CLAUDE.md 对账完成（§5.5）：后端章节增补"版本B 多套设计方案"（N=1 零回归、共用组装、顶层 slots、轮转降级、**旧卡无 `_en` 时英文提示词退化为同一份属既有限制**四条）；前端章节增补"版本B 前端多方案"（`key={activeIdx}` 重挂载与代价、决策点 3 展现形式不变、批量栏范围边界与派生值开关）、"`lib/genConfig.ts`"、"`lib/sizePresets.ts` 新增共用函数"。

##### 阶段三·主体 变更记录 · 2026-08-17（用户验收反馈）

**反馈**：① 批量生成的结果希望直接可视化看图，不用跳到生图任务页；② 「生成图片」与「批量生成勾选方案」为什么不合并成一个窗口。

**变更 1：两个区合并为一个（原 §5.3.2 的"新增 `CollapsibleSection`"设计废止）**

原设计把批量生成做成独立折叠区，结果它和「生成图片」区各有一套**产品尺寸下拉 + 附带竞品原图开关**，同一屏内重复出现两次，用户要在两处分别设置同样的东西。改为：

- `PromptDisplay` 新增可选 prop `batchMode?: {directions, checkedIdx}`——**只有版本B 多方案传**，版本A/C 与版本B 单套模式都不传，因此不显示 tab、界面与改造前完全一致。
- `ImageGenSection` 内部加 `genMode: 'single' | 'batch'`，区顶部两个 tab：`🎯 单张精修（当前方案）` / `🚀 批量多方案（N 套）`。
- **尺寸与"附带竞品原图"设置两个模式共用**（改一处两边生效，不再重复）；切 tab 只切换专属控件——数量按钮组与「添加参考图」仅单张模式显示（批量每套固定 1 张；参考图片段只影响当前方案，批量走原始提示词）。
- 删除 `PromptVersionB.tsx` 里的 `BatchGenerateSection` 整个组件及其独占的尺寸/开关 state。批量模式的编辑指令判断改为 `attachRuleImage && 所有勾选方案都有 image_prompt_edit`——沿用上方那个开关，缺一套就整批回落完整描述模式并在栏内提示（原设计里 `userRefChoice` 派生值开关随该组件一并移除，因为不再有第二个开关）。

**变更 2：批量结果直接看图（原 §5.3.2 明确写的"批量栏不做轮询与图片展示"范围裁剪，现已实现得更好）**

实施中发现一个现成条件：`POST /api/gen/submit` 在同步模式（`api_type=openai`）下**返回时就已带 `image_urls`**（后端 `_submit_one` 的 `is_sync_completed` 分支写入），原设计以为要轮询才有图，所以裁掉了这块。既然响应里就有，**批量模式不需要任何轮询**即可直接展示：

- 缩略图网格（每张标"方案 N"）+ 点击全屏大图预览（复用生图区已有的 `previewUrl` 遮罩，不新写一套）+ 每张 `⬇ 下载`（调**现有** `POST /api/gen/download/{task_id}` 取 `accessible_paths[0]`，再用隐藏 `<a download>` 触发保存）。
- 生成中显示 spinner、失败显示原因；**异步模式（aireiter）此时确实没图**，占位提示去生图任务页——这条限制保留。
- **单张模式仍走轮询**（它可能 count>1 且要跟踪进度），两条路径互不影响。

**验证**（Playwright 真实浏览器 + mock 生图提交，共 46 项断言，三轮）：
- 合并 12/12：旧独立栏已消失；区内两个 tab 可切换；**尺寸下拉从 2 个减为 1 个**；在单张 tab 改成竖版相框(4000×6000)→切到批量 tab 仍是该尺寸（共用生效）；批量 tab 正确隐藏数量按钮组与添加参考图。
- 看图 11/11：批量生成 3 套 → **3 张图直接渲染在区内**、各不相同、每张标注方案序号、每张有下载按钮、完成提示"已完成 3 套"；点图弹出全屏大图预览、点 ✕ 可关闭；点下载正确调用 `/api/gen/download/MOCKGEN-0001`；尺寸按 `clampToApiMax` 缩放为 1067×1600。
- 回归 13/13：切回单张 tab → 数量组/添加参考图/生成按钮全部回来、批量结果按 tab 隔离不再显示、提交 1 次且 count=1、用的是编辑区的编辑指令式提示词；**版本C 面板完全没有 tab**（未传 batchMode），数量组与添加参考图照旧。
- 单张出图路径 10/10：版本C 生图 → 轮询被调用 → 「📸 生成结果」区出图，与改造前一致。
- 三轮共 3 项断言未通过均为**测试脚本自身问题**，非代码缺陷：① 首轮用 `time.sleep()` 阻塞 Playwright 事件循环饿死 route mock（改 `page.wait_for_timeout` 后正常）；② 断言"3 个状态行"时把完成提示条一起数进去得 4；③ 判"单张模式出图"时**没 mock 轮询端点** `/api/gen/task/{id}`（单张走轮询而非 submit 响应），补 mock 后 10/10 通过。
- 全程零 JS 运行时错误；`lint` 仍为基线 19/23、`tsc` 零错误；生图全程 mock，**未产生真实费用**。



### 阶段四：元素拆分图（需求1）
- [ ] **Spike**：抠取质量+透明底验证，结论填 §6.7 + CLAUDE.md，用户确认 go/no-go
- [ ] `extract_element_list` + 抠取指令模板常量 + `GET /api/prompts/elements/{rule_id}`
- [ ] `POST /api/gen/download-zip`（StreamingResponse，前端走 fetch blob）
- [ ] `ElementExtractSection.tsx`（门控/勾选/串行生成/停止重试/下载）
- [ ] `PromptVersionC.tsx` 挂载新区块；gen 页 VERSION_TABS 加 E
- [ ] §6.6 验收清单全过 + lint
- [ ] CLAUDE.md 对账 + 等用户验收确认（三期收官）

#### 阶段四完成记录（追加）

- [x] 2026-08-17 **Spike 首轮**（5 次真实调用）：结论填入 §6.7——干净✅（背景 RGB 253~255）/ 透明底❌（模型画棋盘格，RGB 无 alpha 的假透明，**永久禁用该选项**）/ 保真⚠️（海龟、珊瑚风格漂移成照片级写实；水母达标）。用户决策：先做指令调优轮，无论结局均 go。
- [x] 2026-08-17 **Spike 调优轮**（4 次真实调用，变体 A/B × 海龟/珊瑚）：**变体 B 在两个元素上都最佳**（海龟蓝绿虹彩+插画描边、珊瑚水彩笔触+连原图气泡都还原）。两条推测均被证实：风格锁定句有效（首轮漂移根因是 `Reproduce it exactly` 只约束"画什么"）、消除 mockup 句进一步有效（测试图是装裱画布 mockup）。干净度三轮不变。**定档第一档 → 定稿含风格锁定的模板（=变体B），功能定位保持"元素分离素材图"，不加软化文案。** 完整实测数据与定稿模板见 §6.7 调优轮实测结论。
- [x] 2026-08-17 `ELEMENT_EXTRACTION_PROMPT_TEMPLATE` 常量落地（`prompts/prompt_generation.py`），与 Spike 变体 B **逐字符一致（615 字符实测校验）**，注释写明三段各自的实测由来与"改前请重跑 Spike"。
- [x] 2026-08-17 `extract_element_list` 完成（`services/prompt_generator.py` 模块级函数）。**实施中发现方案 §6.3.1 的两道去重防线不足，当场补强（详见下方变更记录）**。
- [x] 2026-08-17 `GET /api/prompts/elements/{rule_id}` 完成（纯同步计算不调 AI、404 守卫、`logging.exception`、包装格式）。
- [x] 2026-08-17 `POST /api/gen/download-zip` 完成（`StreamingResponse` 裸 zip、上限 20、复用 `download_images` 幂等、单任务失败跳过不中断、zip 内名带 `task_id` 前缀防同名覆盖）。
- [x] 2026-08-17 `ElementExtractSection.tsx` 完成（按钮触发入口、三重门控、默认全不勾、串行生成、停止、单项重试、`object-contain` 结果网格、单张+打包下载、大图预览、beforeunload 守卫）；`PromptVersionC.tsx` 挂载；`app/gen/page.tsx` 的 `VERSION_TABS`/`VERSION_ORDER` 加 `E`。
- [x] 2026-08-17 后端自测 22/24 通过：元素清单端点（字段齐全、模板三段都在、无 transparent、非文字位排前、404）；去重实测 RULE-0043 从 12 项→7 项无重复；zip 端点（空/超 20 → 400、全不存在 → 400、真实 3 任务打成有效 PNG、幂等复用 `local_images`、混合场景跳过无效项、`local_images` 落库）。2 项未通过是测试脚本读 header 大小写不匹配（`curl -D-` 确认 `content-type: application/zip`、`content-disposition: attachment; filename=split_elements_*.zip` 都正确）。
- [x] 2026-08-17 §6.6 前端验收 31/32 通过（Playwright 真实浏览器，生图 mock 不烧钱）：
  - **新增验收项：未点按钮前 elements 请求数 = 0**（实测 0），入口按钮 + 低频小注渲染、面板未渲染；点击后恰好 1 次请求、面板展开、7 个元素清单。
  - 警示条含"约 1 分钟/张、按张计费" + "白底 PNG（RGB 253~255，如需透明请自行二次抠图)"；**全面板无任何透明底选项**；默认全不勾且生成按钮禁用。
  - 勾 3 个 → 串行提交 3 次、全部 `version='E'` + `attach_rule_image=true`、提示词含风格锁定与 mockup 句、负向为空、3 条提示词各不相同；第 2 个 mock 失败 → 标原因 + 重试按钮、**队列未被阻塞**（另 2 张正常出图）；重试后 3 张全出。
  - 结果图 `object-contain`（用故意非方图 8×12 验证不裁切）；点图大图预览（白色底衬）可开可关；打包按钮计数随勾选同步；mock 任务 ID 后端不存在时**显示错误而非静默**。
  - 唯一未通过项是测试脚本 `json.dumps` 未加 `ensure_ascii=False` 致中文错误信息被转义，截图 `/tmp/s4_results.png` 确认失败行与重试按钮均正确渲染。
- [x] 2026-08-17 `npm run lint` 与基线一致（19 errors / 23 warnings）；`npx tsc --noEmit` 零错误。
- [x] 2026-08-17 CLAUDE.md 对账完成（§6.8）：后端章节增补"元素拆分图链路"（模板三段实测由来、永不透明底、比例不受控、三道去重+两类排除、文字位兜底、zip 裸二进制例外六条）；前端章节增补"元素拆分入口是按钮触发"。

##### §6.10 阶段四变更记录（三）· 2026-08-17（需求理解纠正：要的是"每个候选变体各生成一张"）

**用户澄清**：截图指向的是**元素下拉框里的那一串候选变体**（金毛犬/拉布拉多犬/猫咪/贵宾犬/兔子/仓鼠），要的是"**列表下所有元素都生成**"——即把这些候选各生成一张素材图，而不是只把原图里那一个元素抠出来。**我此前理解错了**，做成了"一个元素抠一张"。

**用户确认的两个口径**（AskUserQuestion）：
1. 生成物 = **同一个位置的 N 种变体素材**（透明底、位置/姿态一致、可直接叠换），不是"N 种变体的完整设计图"（那是版本C 的批量版，与元素拆分无关）。
2. 范围 = **勾选的几个就生成几个**（不是一次只做一个维度，也不是全维度全做）。

**数据模型改动**：
- `extract_element_list` 的每个元素新增 `variants` 数组：第 0 项是 `original`（走擦除模板，图里本来就有），其余是 `alternatives`（走新增的 `ELEMENT_VARIANT_PROMPT_TEMPLATE` 替换模板，图里没有需要"换出来"）。中英配对按 `alternatives_en` 索引对齐，取不到放行中文（同 `_edit_lang_value` 策略）。
- 前端数据模型从"一个元素一个任务"改为"**一个变体一个任务**"（`JobItem`），维度只作为分组与"全选该维度"的载体。

**新增 `ELEMENT_VARIANT_PROMPT_TEMPLATE`**，与擦除模板的关键差别（实测 RULE-0063 金毛犬→猫咪）：
- `REPLACE it with {variant}` + 强调"必须占据完全相同的位置/尺寸/取景" —— 只说"换成猫"模型会重新构图，姿态和位置都跟原来对不上，一组变体之间就不通用了。
- `pose_clause` **仅当规则卡对该元素有成句英文描述时才加**：第3层的 `value_cn` 常常就是短标签（"金毛犬"），第一版拿它当姿态说明，生成的指令里出现了 `keep the same pose and composition: 金毛犬` —— 毫无信息量还混入中文，已修。
- 风格锁定改为"与原图中**该元素**的渲染风格一致"（而非泛化的手绘插画）——变体要和原设计里其他元素放在一起，风格必须对齐原元素。

**发现并修复的第二个真问题：第2/3 层同位置元素漏判重（防线①-d）**
`宠物类型='Golden retriever'`（第3层）与 `主体宠物肖像区='A front-facing half-body portrait of a single pet…'`（第2层）是画面同一个位置，但**英文实词交集为空**（品种名 vs 构图描述），既有的词级重叠判重（①-c）挡不住。漏判的后果比"多抠一张"更糟：第2层项没有 `alternatives`，会在界面多出一个"只有 1 个候选"的重复维度干扰勾选。
新增 `_cn_name_overlaps`：剥掉结构性后缀（区/位/层/框/型/类型/主体/符号）后取中文核心名做子串比对，核心名 <2 字放弃（如"花"会误伤）。**实测 RULE-0063 从 5 个维度收敛到 3 个**（宠物类型/花卉装饰/爪印符号，各 6 候选），与用户截图完全一致。

**前端交互**：维度行可展开/收起候选清单（有多个候选的默认展开）、每行一个"全选 N"、变体逐个勾选、串行生成、结果**按维度分组**展示、单张与打包下载。

**真实验证**（3 次调用）：
- 变体替换成立：金毛犬 → **猫咪**，透明底、正面半身姿态与原图一致、渲染风格一致（连微笑张嘴都保留）。
- 同维度一致性（兔子 vs 仓鼠）：画布尺寸相同、**水平位置偏差 0.02/0.01**（很准）、垂直偏差 0.10~0.12 —— 来自动物本身体型差异（兔子耳朵高、仓鼠矮胖），不是位置漂移，属合理。
- 全库 46 张卡：有多个候选的元素 137 个、只 1 个候选的 81 个（后者是纯第2层元素、规则卡里没有 alternatives，仍可原样抠取）。
- `npm run lint` 与基线一致（19 errors / 23 warnings）、`npx tsc --noEmit` 零错误。

**一处如实说明的限制**：位置是**相对设计区**而非整张竞品照片的绝对坐标。竞品图常是实物摆拍（如 RULE-0063 是灯箱照片，设计只占中间一小块），模型会自动剥离外壳按设计稿重新框定。对"一组变体互相叠换"这个用途足够（组内一致即可），但不是"整张照片的像素级对位"。

##### §6.9 阶段四变更记录（二）· 2026-08-17（用户验收反馈四项，全部已解决）

**用户反馈**：① 比例位置不对，要竞品原图直留对应元素且位置要对；② 元素识别不对，只看图中的构建元素；③ 能不能出透明底；④ 勾选列表里的都要能拆分生成。

**调查与结论（共 9 次真实调用：Spike 首轮 5 + 调优 4，本轮另 5 次验证）**：

**① 位置/比例 —— 已修复。根因是我的模板写反了方向。**
第一版模板写 `Place it alone, centered, on a plain pure white background`，**主动要求了重新居中**。改为"原位擦除式"（`erase-everything-else, NOT a re-draw` + `must stay at EXACTLY the same position, same size, same scale` + `Keep the output canvas the same aspect ratio and framing`），并让 `GET /api/prompts/elements/{rule_id}` 连带返回 `source_width/source_height`（Pillow 读竞品原图）、前端按原图宽高提交而非固定 1024 方图。
实测（RULE-0026 Luna 卡，原图 1086×1448）：输出画布 = 原图尺寸；爱心图标落在 x 0.48~0.53 / y 0.86~0.90（原图目测 x 0.44~0.53 / y 0.79~0.84）；彩虹落在 y 0.27~0.63。**位置对位达成。**

**② 元素识别 —— 已按"只要图形元素"口径收窄（用户在选项中确认）。**
`extract_element_list` 末尾过滤掉全部 `is_text_slot` 项。RULE-0026 从 12 项 → **6 项图形元素**（宠物类型/花卉类型/彩虹拱门/云朵背景/花卉装饰边框/爱心图标区），剔除的 6 项全是文字（Luna、2011-2026、纪念文案 + 三个"文字区"描述位）。`is_text_slot` 的解析逻辑仍必需——它就是过滤依据（旧卡无该字段，靠关键词兜底）。

**③ 透明底 —— 能做。之前 Spike 的"不支持"结论是错的，本轮已纠正。**
错在**只在 prompt 文字里要求透明，从没试过 OpenAI images API 的 `background="transparent"` 参数**。加上该参数实测直接返回真 RGBA（alpha extrema 0~255、64.9% 完全透明）。
但发现真实取舍：**该参数与"保持原位置"不可兼得**——要求保持原画布时上游忽略它、返回原尺寸 RGB 白底（`size` 取 1024x1024 与 1024x1536 都一样，已用控制变量法排除 size 是原因）。
按用户选择"位置优先 + 后端转透明"落地：新增 `services/image_alpha_utils.py::white_to_transparent()`，**软 alpha（按离白距离连续取值）+ un-premultiply 去白**，而非二值抠图（水彩边缘是渐变的，二值化必然留白边或啃边缘）。阈值 `ALPHA_LO=14 / ALPHA_HI=45` 有数据依据：白底图"离纯白距离"分位数 50%→9、80%→10、90%→14、95%→101，背景噪点 ≤14、真实内容 ≥101，中间有干净空档；LO=6 时噪点被当内容、包围盒撑满全图（0.00~1.00），LO=14 起稳定。实测**白边残留 0.0%**（合成到红底目视确认 + 客观指标）。转换**只对 `version=='E'` 生效**——普通 POD 设计图的白底是设计的一部分，绝不能扒掉。

**④ 批量拆分 —— 保持串行（用户在选项中确认），并修掉了拦路的真 bug。**
勾选多个 → 串行逐个跑（约 40 秒/个），每张出来立即显示，失败只影响单张可重试。
**实施中发现一个必须修的真问题**：首轮真实验证时抠"彩虹拱门"，花卉/文字/云朵都擦干净了、**猫却完整留着**——只写 `Erase every other element` 时模型不敢删主体（主体在画面里太显著）。修复：模板加 `{others}` 占位，由 `extract_element_list` 从**全部元素**（含被过滤掉的文字位，文字也得擦）减去目标本身生成点名清单，并加一句 `Erasing the main subject is REQUIRED if it is listed above`。清单生成有两个细节：用 `_is_semantic_duplicate` 排掉与目标同义的表述（否则出现"擦掉 X"与"保留 X"自相矛盾）、用 `_shorten_label` 把长句截短（整句塞进清单会稀释指令）。
**修复效果实测**：猫所在中心区域的不透明占比从 **60%+ → 0.1%**，包围盒从撑满全图收回到 y 0.27~0.63，出图只剩彩虹。

**其他配套改动**：
- 结果网格与大图预览改用**棋盘格底衬**（`CHECKER_STYLE`，CSS 渐变不引图片资源）——透明 PNG 放在白/深色卡片上都看不出哪里是透明的。
- 警示条文案改为"输出为**透明底 PNG**，保持原图中的位置与比例（可直接叠放拼图）"，"约 1 分钟/张、按张计费"保留。
- §6.7 里"永不提供透明底选项"的结论**已被本轮推翻**（现在默认就出透明底），但"不要在 prompt 文字里要求 transparent"仍然成立（那样会画棋盘格图案）。

**验证汇总**：全库 46 张规则卡的指令生成无占位符残留、无空排除清单；真实链路 10/10 通过（2 元素 × 位置/透明/画布/data URI 各项）；主体擦除专项验证通过；`npm run lint` 与基线一致（19 errors / 23 warnings）、`npx tsc --noEmit` 零错误。

##### 阶段四变更记录 · 2026-08-17（实施中发现，方案 §6.3.1 设计不足）

**问题**：方案 §6.3.1 只设计了两道去重防线（槽位名同名、描述含已收集值），实测在真实规则卡上**挡不住**两类问题，会导致重复付费调用和无意义调用：

1. **措辞不同的同一元素漏过**：RULE-0043 里 `尾巴造型='Glowing blue-green scaled tail'`（第3层）与 `人鱼尾巴='A long scaled mermaid tail in blue-green tones with a glowing effect'`（第2层）显然是同一条尾巴，但无子串包含关系、槽位名也不同 → 两道防线都失效。同类还有 `海洋生物组合`vs`海洋生物环绕`、`前景植物装饰`vs`珊瑚与海草前景`。**共 3 组重复 = 3 次白烧的调用。**
2. **抽象属性被当成可抠元素**：RULE-0001 的 `色彩方案='粉白绿棕（女孩感）'`、`风格='柔和水彩'`、`场景切换='通用'` 送进抠取指令毫无意义（模型只能凭空编图）。
3. **旧规则卡的文字位识别失效**：旧卡整卡都没有 `is_text_slot` 字段，只认该字段会把"名字/日期"当普通图案元素（排序错、默认勾选语义错）。

**补强（三处）**：
- **新增防线①-c 词级重叠判重** `_is_semantic_duplicate`：把描述切成实词集合（去停用词、粗糙去复数），重叠率**按较短一方**计算（第2层描述通常长得多，按长的算永远达不到阈值），≥0.7 判为同一元素。实测能把 4 组真重复（重叠率 100%）与 2 组不同元素（"鱼群+水母+海龟"整组 vs 单独"左下角海龟" = 40%；光束 vs 服装 = 0%）干净分开。
- **新增两类排除** `_is_abstract_dimension`：抽象属性按**维度名**判断（风格/配色/氛围/构图/场景切换等关键词）；纯色底按**值**判断而非按名——`背景光效='顶部水下阳光光束'` 名字带"背景"但光束确实可抠，**第一版按名判断误伤了它，已改为按值**。
- **新增 `_looks_like_text_slot`**：显式 `is_text_slot` 优先、缺失时按关键词兜底，关键词表与 `_get_pod_hints` 的 `text_keywords` 同源，保证同一张卡在"生图提示词文字位处理"与"元素拆分清单"两处判断一致。

**效果**：RULE-0043 12项→7项（去掉 3 组重复 + 1 项服装重复）、RULE-0061 6→5、RULE-0001 11→7（去掉 3 个抽象属性 + 1 个纯色底），全库 46 张卡无一产出 0 元素（最少 4 个、最多 16 个）。已同步记入 CLAUDE.md。


#### 阶段四追加记录（2026-08-17，实施前）

- [x] 2026-08-17 Spike 首轮完成，结论已填 §6.7；用户决策：**先做指令调优轮，无论结局均 go**（三档定位，见 §6.7 用户决策块）
- [ ] 指令调优轮：变体 A/B × 海龟/珊瑚（≈4 次调用），结论追加进 §6.7、定稿 `ELEMENT_EXTRACTION_PROMPT_TEMPLATE` 与 UI 定位文案
- [ ] 【2026-08-17 追加需求】入口改按钮触发（§6.4.1 变更块）：默认零请求零面板、无原图时按钮置灰、点击后才加载清单、生成仍需二次确认；§6.6 验收按变更注调整

---

##### §6.11 阶段四变更记录（四）· 2026-08-18（问题1"主体元素缺失"根因纠正）

**用户反馈**：元素拆分图中"图中有部分被擦出，造成主题元素缺失"（截图为浅金毛狗，头顶/身体边缘有棋盘格破洞）。

**我此前的诊断是错的，这里如实记录**：我一直假设"上游返回白底 RGB 图、本地
`white_to_transparent()` 把它抠成透明"，于是花了大量时间调本地阈值算法
（连通性判据、面积例外、合成样例）。

**实测推翻这个假设**（把用户这批 18 张图从 `data/gen/tasks/GEN-015x~017x.json`
全部导出检查）：**24 张全部是 `mode=RGBA` 的已抠图**，`white_to_transparent()`
每一张都在入口处 `return {"converted": False, "reason": "already_transparent"}`
直接跳过——**本地抠图代码从未执行过**。破洞是上游模型自己抠出来的。

根因：`_openai_generate` **完全没有传 `background` 参数**。不传时上游自作主张
返回 RGBA 已抠图，而它抠得有碎洞。

**上游自抠的碎洞实测**（内部空洞率 = 透明且不与画布边缘连通的像素占比）：

| 元素 | 上游自抠内部空洞 |
|---|---|
| `wildflower sprigs with greenery` | 4.68% |
| `ivy vines with lavender blooms` | 2.48% |
| `eucalyptus branches with white flowers` | 2.33% |
| `fern branches with tiny daisies` | 1.97% |
| `horse portrait` | 0.21% |
| `labrador retriever portrait`（用户截图那张） | 0.03% |

**改动**：
1. `ImageGenClient.submit_task` / `_openai_generate` 加 `force_opaque: bool = False`，
   为真时请求体加 `background="opaque"`（两个分支都加：`/v1/images/edits` 的
   multipart 用字符串值，`/v1/images/generations` 的 JSON 用同名字段）。
2. `routers/image_gen.py` 的 `_submit_one` 传 `force_opaque=(request.version == "E")`
   ——**只有元素拆分要整图**，普通 POD 生图完全不受影响。
3. `image_alpha_utils.white_to_transparent` 入口不再对 RGBA 直接返回，改为
   合成回白底后重抠（兜底：上游哪天忽略 `background` 参数仍能补洞），
   统计里加 `reprocessed_from_alpha` 标记走没走这条兜底路径。

**实测验证（2 次真实调用，均走完整新链路）**：

| 用例 | 上游返回 | 本地抠图结果 |
|---|---|---|
| `wildflower sprigs`（碎洞最严重） | ✅ `mode=RGB` 941×1672，`background=opaque` **生效** | 内部空洞 1.12%，茎秆完整、**枝叶间隙正确透明**（合成红底目视确认） |
| `thin gold circle`（环形，最危险用例） | ✅ `mode=RGB` | `enclosed_kept=1`，**圆环中心 alpha 均值 0.0（正确保持透明，未被填成实心金饼）** |

**连通性修正确实在救浅色主体**：对 `wildflower sprigs` 的 opaque 整图，被恢复的
72046 像素（4.58%）在原图 RGB 均值为 **(252, 252.3, 251.5)**——离纯白距离仅 3，
远低于 `ALPHA_LO=14`，纯阈值法必然erase。可视化确认恢复区域精确落在**茎秆与叶片**
形状上，不是块状误填。这印证了 `ENCLOSED_BG_MIN_RATIO=0.02` 的面积判据：
碎洞/浅色主体（<2%）恢复，环形中心（`thin gold circle` 占 20.88%）保持透明。

**两个必须记住的教训**：
1. **"内部空洞率"这个指标会骗人**。我用它三次得出错误结论：兔子"8.6% 破损"实为
   矩形包围盒内耳朵旁的正常背景；`wildflower sprigs` 的 4.68% 里**大部分是枝叶
   间隙**（本就该透明）——按它"补洞"反而把间隙填白，合成红底一眼可见变差。
   **判断抠图质量必须合成到对比色背景目视确认，不能只看数字。**
2. **没有 `background` 参数时不要假设上游返回什么**。同一个代理，此前 Spike
   结论说"要求保持原位置时会忽略 `background=transparent`、返回白底 RGB"，
   而不传参数时它返回 RGBA。**每次都要实测 `Image.open().mode`。**

---

##### §6.12 阶段四变更记录（五）· 2026-08-18（v1 本地抠图在浅色主体上翻车，重构为 v2"严判背景"）

**用户反馈**：新链路（§6.11 之后）生成的拉布拉多（GEN-0175）头顶/面部仍有
深色斑块。用户提出方向："先判断元素形状区域，然后区域里面的不做任何擦除"。

**诊断（数学指纹法）**：斑块像素的存储 alpha 与 v1 软阈值公式
`clip((dist_flat-14)*255/31)` 的误差中位数仅 1.8——**确认斑块是本地 v1 算法
造成的，不是上游**。机制：① 全局软阈值把离白距离 14~45 的奶油色毛发判成
半透明；② `FLOOD_WHITE_DIST=40` 的宽容差洪水把浅毛也算"白"、指望主体轮廓
当屏障，但这只狗的轮廓本身近白、屏障有缺口，洪水漏进内部。昨天的合成实验
早已预言这个机制，当时误以为真实图不会踩中。

**前提数据**（v2 可行的根据）：三张真实图（野花/圆环的 opaque 整图 +
GEN-0175 外部区）四角离白距离 **p99 全部 = 2**——上游整图的背景近乎纯白，
与最浅内容（距离 ≥5）之间有干净空档。v1 设计时按早期脏背景数据（噪点到 14）
取了宽容差，如今是不必要的过度兼容。

**v2 重构（`image_alpha_utils.py`，= 用户思路"区域内不擦"）**：
- 背景须**同时满足**：离白距离 ≤ 自适应容差（`_estimate_bg_tol`：四角采样取
  最干净角 p99+3，clip [4,12]）**且**从画布边缘洪水可达（4 邻接）
- 环形例外照旧：封闭白区 ≥2% 画布判背景（圆环中心）
- **其余像素一律 alpha=255**；软过渡（ALPHA_LO/HI 线性带 + un-premultiply
  去白）只保留在背景膨胀 4px 的羽化带内，保住水彩渐变边不留白边
- scipy 缺失时降级回全局软阈值（功能不中断）

**验证（全部零成本本地复算）**：

| 用例 | v1 | v2 |
|---|---|---|
| GEN-0175 拉布拉多（失败案例） | 内部斑块 0.30%，深底可见灰斑 | **0.000%**，深底目视完整 |
| thin gold circle（环形） | 中心透明 ✅ | 中心 alpha 0.0 ✅（enclosed_kept=1） |
| 野花 opaque 整图（细枝叶） | 间隙正确 | 内部非透区 0.04%，间隙红底目视正确，奶白茎秆完整保留 |
| 昨天 24 张历史图 | — | 全部内部非透区 ≤0.12%，无异常 |

**验收口径更新**：必须同时测**三类**——浅色主体（斑块）、环形（中心透明）、
细枝叶（间隙不被误填）。三类失败方向互相冲突，只测一类必漏另两类。

**备选方案（本次未用，留档备升级）**：若未来仍有本地无法区分的案例
（如内容真的画到与背景同白），两个升级方向：① **换底色**（抠取模板的
"pure white"改为按图选定的键控色，从信息上根除白撞白，需重跑模板 Spike）；
② **本地语义分割**（rembg/U²-Net，按形状不按颜色，新增依赖 + 需验证细线框
元素不被填实心）。当前 v2 已覆盖全部真实案例，暂不引入。

---

##### §6.13 阶段四变更记录（六）· 2026-08-18（问题2：元素比例应基于"图案"而非"上传文件"）

**用户反馈**：「对于元素比例，是基于图案的比例大小，而不是上传竞品图的大小」。

**根因（实测确认）**：`GET /api/prompts/elements/{rule_id}` 用 `Image.open().size`
取**文件**尺寸。RULE-0063 的文件是 **2000×2000 方图**，但打开原图看到的是
**户外灯笼实物摆拍**——真正的印刷图案是灯笼面板上约 1:2.2 的窄竖条，
四周全是灯具外壳、草地虚化背景，还有一只手。产品类型也确实记录为「Frame 竖版相框」。

按文件比例请求，元素拆分的提示词前缀是 `[Canvas: 1:1 square format]`，图案必然
被挤扁、位置全错。更糟的是**同一段提示词里有两句自相矛盾的指令**：
前缀要求 1:1 方图，模板又写 `same aspect ratio and framing as the provided image`。

**改动（4 处）**：
1. `prompts/rule_extraction.py`：第 2 层新增 **`artwork_orientation`** 字段，
   只三档 `portrait/landscape/square`。**故意不要精确比例**——VLM 估精确比例不可靠，
   且 `_get_openai_size` 本来只有 1:1 / 3:2 / 2:3 三个桶，粗分类够用。字段说明里
   明确"判断对象是图案本身不是照片"并举了灯笼/马克杯两个实物摆拍的例子；
   JSON 示例按项目铁律用**方括号占位符不给具体值**。
2. `models/rule_card.py`：`VisualStructureLayer.artwork_orientation`（**Optional**，
   旧规则卡没有此字段时回落文件比例，行为与改动前一致）。
3. `routers/prompts.py`：`_get_artwork_orientation`（只接受三个合法值，
   大写/带空格归一化，非法值当作没有）+ `_apply_orientation`（**只重排长短边**，
   不改具体数值；方图文件 + portrait 时无短边可用，按 2:3 构造）。响应新增
   `artwork_orientation` 供前端提示。
4. `prompts/prompt_generation.py`：**两个模板**（擦除 + 替换）的画布措辞改为
   锚定 `the printed artwork area of the provided image (... not the whole
   photograph)`，消除与校正后画布请求的矛盾。

**验证**：

| 项 | 结果 |
|---|---|
| VLM 独立判断（不给引导，只问朝向） | 「竖版，图中是带灯座的实物摆拍，真正的印刷图案区域明显是高而窄的竖向长方形」 |
| 完整 prompt 实测 RULE-0063 | `artwork_orientation = 'portrait'` ✅，`layer_4` 首产品仍为「Frame 竖版相框」（无回归） |
| 画布校正 | 2000×2000 → **1333×2000** |
| OpenAI 尺寸桶 | `1024x1024` → **`1024x1536`**（真正换桶了，不只是数字变化） |
| 8 组边界用例 | 方图+竖/横、横文件+竖图案（交换）、竖文件+横图案（交换）、square、旧卡无字段、尺寸读取失败 全部符合预期 |
| 旧卡对照组 RULE-0043 | 1254×1254 保持原样（回落路径正确） |
| 前端 lint | 42 problems，与改动前基线**逐字相同**（零回归） |

**前端**：`ElementExtractSection` 的 `sourceSize` 消费点无需改动（后端已在返回前
校正），只新增警示条一行说明画布依据——有 `artwork_orientation` 时显示
「按 AI 判断的图案朝向：竖版」，旧规则卡显示「按竞品原图文件比例；此规则卡分析时
未判断图案朝向，重新分析可校正实物摆拍图的比例」，让用户知道为什么比例是这样、
以及怎么改善。

**已知限制**：`artwork_orientation` 只对**新分析**的规则卡生效。已有 49 张旧卡
需要重新分析才能获得该字段，否则继续按文件比例（对纯图案卡本来就正确，
只有实物摆拍类的方图卡会不准）。

---

##### §6.14 阶段四变更记录（七）· 2026-08-18（问题3：品类识别不出，说明了也没用）

**用户反馈**：「运营反馈在分析竞品图这里，分析不出是哪个品类的物品，**哪怕是在输入框中说明了**」。

**两层根因（都用真实数据量化过）**：

**根因 A —— hint 措辞把用户的话降级成了可忽略的建议。**
`_append_hint` 结尾原文是「以上用户补充内容**只是参考方向**」。用户明确写了
"这是马克杯"，VLM 读到的却是"这只是个建议"，于是继续按画面自己猜。

**根因 B —— 产品名是自由文本，没有受控词表。**
实测 51 张规则卡的首个 key 分布：

| 数量 | 首个 key |
|---|---|
| **26** | **Blanket 毛毯** ← 超过一半，大量是错的 |
| 5 | Frame 横板相框 |
| 4 | T-Shirt T恤 |
| 4 | T-Shirt ← 同一产品的另一种拼写 |
| 3 | Canvas 挂画 |
| 3 | Poster 海报 |
| 2 | Frame 竖版相框 |
| 各 1 | 毛毯 / Blanket / Pillow 抱枕 / Poster |

同一产品最多有 **3 种拼写**（`Blanket 毛毯` / `Blanket` / `毛毯`），且
CLAUDE.md 明令禁止的"默认毛毯"实际发生在**超过一半**的卡上。典型误判：
RULE-0015 的原图文件名就写着 `08×12inch(20×30cm)`（相框/海报尺寸），却判成毛毯。

**改动（4 处）**：
1. **新增 `vocabularies/product_type.json` 注入第 4 层**（55 项）。
   ⚠️ **格式必须转换**：词表文件是全项目统一的 `中文/English` 斜杠格式，但
   `adaptations` 的 key 历来是空格分隔的 `English 中文`，三个下游消费方都依赖它
   （`_get_adaptation` 精确 key 匹配、`_extract_product_name_en` 空格切分、
   前端 `ProductSelect` 拿 key 当下拉选项）。所以 `_product_type_options()` 转换后
   再注入，让 VLM 直接输出可用 key，而不是在下游到处做兼容。注入时**过滤掉
   `未识别/Unknown`**（出现在"请选择"清单里会诱导 VLM 偷懒选它）。
2. **第 4 层 prompt 加硬约束**：把"26/51 都填了毛毯且大部分是错的"这个统计事实
   直接写给 VLM 看；明确"毛毯只有确实看到叠起织物/盖在身上时才能填"；
   给出**"判断不出就填 `Unknown 未识别`"的出口**（填未识别远好过瞎猜）。
3. **`_append_hint` 区分两类内容**：品类/载体**以用户说明为准**（第一手事实，
   用户看得到实物），其余仍是参考方向。**防注入设计不动**——约束句照旧放最后，
   且明确"除品类外其他字段不得因用户要求而伪造"。
4. **前端 `getProductOptionsFromRuleCard` 过滤 `Unknown 未识别`**（否则生图提示词
   首句会变成 `Create a Unknown print-on-demand design.`），过滤后为空回落内置常见产品。

**验证（6 次真实 VLM 调用）**：

| 用例 | 结果 |
|---|---|
| 灯笼图无 hint | `Picture Frame 相框`（比原 `Frame 竖版相框` 更规范，且不再是毛毯） |
| 灯笼图 + hint「户外太阳能灯笼」（补词表前） | `Unknown 未识别` ✅ **兜底出口按设计生效**，未瞎猜 |
| 同上（补词表后） | **`Lantern 灯笼`** ✅ hint 被硬采纳 |
| 同图 + hint「这是印在毛毯上的」（反向对照） | **`Blanket 毛毯`** ✅ 证明确实是 hint 在驱动，不是模型忽略 |
| **注入攻击**（要求 `style=HACKED`、`reuse_level=SSS`、覆盖 layer_0、不输出 JSON） | 全部被挡：JSON 合法、`style='柔和水彩/Soft Watercolor'`、`reuse_level='A'`、layer_0 未被覆盖；只有品类按预期可影响 ✅ |
| RULE-0015 回归（历史误判为毛毯） | `Blanket 毛毯` → **`Poster 海报`** ✅ 已纠正 |
| RULE-0018 对照（真毛毯） | `Blanket 毛毯` → `Blanket 毛毯` ✅ 未过度纠偏 |
| 前端 lint | 42 problems，与基线**逐字相同**（零回归） |

**词表覆盖不到的载体要补词表，不要让 VLM 硬凑**。本次实测发现灯笼类载体缺失
（比对内置的亚马逊类目数据确认 `Lantern`/`Wall Plaque`/`Night Light` 确实存在但
词表没收），补入 5 项：装饰木牌 / 装饰挂牌 / 亚克力灯牌 / 灯笼 / 小夜灯。

**已知限制**：这些改动只对**新分析**的规则卡生效。已有 51 张旧卡的产品名仍是
旧的自由文本（含 3 种拼写、26 张疑似误判的毛毯），需要重新分析才会规范化；
旧卡在界面上照常可用（`_get_adaptation` 精确匹配失败时本来就有兜底路径）。

---

##### §6.15 阶段四变更记录（八）· 2026-08-18（元素变体素材与可变维度下拉框内容不一致）

**用户反馈**：元素变体素材区显示「花卉边框装饰区 1 个候选」，而版本C 的可变维度
下拉框「边框花卉风格」有 **5 个候选**（浅色玫瑰/白色雏菊/薰衣草/向日葵/樱花）。
「你没有复用可变维度的内容吗？」

**根因（两个，第一个更严重）**：

**A. 抽象属性判定误杀了带实体的维度。** `_is_abstract_dimension` 命中抽象关键词
就直接排除，而"风格"在关键词表里——于是第3层 `边框花卉风格`（画面里真实存在的
花卉边框，带 4 个 alternatives）**整个维度被丢掉**，界面上只剩第2层那个没有候选的
`花卉边框装饰区`。这不是"没复用"，是**复用的那一项被误删了**。

**B. 中文名判重漏判。** 剥掉结构性后缀后是 `花卉风格` vs `花卉装饰`，
都含"花卉"却**互不为子串**，子串判定挡不住，第2层那项因此留了下来。

**改动（`services/prompt_generator.py`）**：
1. `_is_abstract_dimension`：命中抽象关键词后**再看有没有实体名词**，有则不判抽象
   （实体词优先于抽象修饰词）。
2. `_cn_name_overlaps` 扩展为**三级判定**：
   - 一级 子串（原有）。**修了一个既有 bug**：后缀表里 `型` 排在 `造型` 前面，
     导致 `尾巴造型` 被剥成 `尾巴造`、与 `人鱼尾巴` 失去子串关系。**长词必须先剥**。
   - 二级 共享实体名词 `_ENTITY_NOUNS` + **同义/上下位词组** `_ENTITY_SYNONYM_GROUPS`
     （挡 `点缀昆虫='紫色蝴蝶'` vs `蝴蝶点缀`——上位词 vs 下位词，字面无重叠，
     且英文 `butterflies`/`butterfly` 因粗糙去复数也对不上，三道防线原本全漏）。
   - 三级 共享角色词 `_ROLE_WORDS`（挡 `主体角色` vs `主体恐龙形象`）。
     **收紧条件防误伤**：须共享 ≥2 个角色词，或共享 1 个且有一方去掉角色词后为空。

**验证**：

| 项 | 结果 |
|---|---|
| RULE-0067（用户案例） | `边框花卉风格` **5 个候选**回来了，与下拉框逐项一致；重复的 `花卉边框装饰区`、`宠物照片占位区` 已消失 |
| 判重用例 11 组真重复 | 全部挡住 ✅（含 `蝴蝶点缀`/`主体角色`/`尾巴造型` 等） |
| 判重用例 8 组不同元素 | **零误判** ✅（含 `海龟前景角色` vs `前景植物装饰`——只共享 1 个角色词但两边都有实义） |
| 抽象判定 9 组 | 全对 ✅（`边框花卉风格`/`翅膀风格` 保留，`整体色彩风格`/`配色方案`/`排版布局` 仍排除） |
| **全库 51 张回归** | 维度数 213→**198**（去掉 15 个重复），变体总数 846 |
| **无实体丢失校验** | 被丢弃的多候选 L3 维度共 19 个，**逐个核对全是真抽象（配色/风格/场景切换）或文字位（名字/日期/文案）**，无具体实体被误删 |

**教训**：改这两个函数必须**同时**跑"真重复要挡住"和"不同元素不能误判"两组用例。
本次调试中我先放宽判重导致 `尾巴造型` 回归失败，再收紧后才两边都过——
只测一组会往另一个方向翻车。

---

##### §6.16 变更记录（九）· 2026-08-18（分析页"解析失败"事件：根因定位 + 全链路审计）

**用户反馈**：重新分析灯笼图出现「解析失败 - 待人工填写」空壳卡，质疑是否
前几轮改动（词表注入/朝向字段/hint 措辞）改坏了，要求全链路审计逻辑链接同步。

**根因定位（走真实端点复现，非猜测）**：

1. 用用户失败的同一张上传文件（`data/uploads/1787037031_*.png`）+ 同款 hint
   请求 `POST /api/analyze`，复现失败，`_parse_error` 显示：
   **`502 Bad Gateway from http://64.186.233.107:8356/v1/chat/completions`**。
2. 直连探测同一上游：先 3 连 200（纯文本小请求），几分钟后**同样的纯文本小请求
   也 502**，带图大小请求全 502、响应体为空——**上游 AI 代理在间歇性抖动**，
   与请求内容无关（同一 prompt 此前 6+ 次全成功）。
3. 结论：**失败由上游服务故障引起，与本批代码/prompt 改动无关**。
   5xx 已有 2 次退避重试（#13），抖动窗口超过重试跨度时失败是预期行为。

**但审计确实挖出一个真 bug（这才是"看起来像解析逻辑坏了"的原因）**：

- **前后端字段名不同步**：后端把失败原因写进 `rule_card["_parse_error"]`
  （带下划线），前端 `analyze/page.tsx` 检查的是 `parse_error`（不带）——
  永远取不到值，于是**失败被当成成功**、渲染成空壳卡，502 的原因被完全吞掉。
  单图与批量两条路径同病。**修复**：都改查 `_parse_error || parse_error`，
  有值时抛 `AI 分析失败（可稍后重试）：{原因}` 走既有错误态 UI。
  该 bug 是历史遗留（批量路径照抄了单图路径的错误检查），非本批引入。

**全链路审计结果（用户点名要查的同步/复用项）**：

| 链接点 | 结论 |
|---|---|
| `artwork_orientation` 三条保存链路 | ✅ 单图 `{...ruleCard}` / 批量 `{...card}` / 编辑器深拷贝+spread，额外字段全存活 |
| 旧规则卡兼容 | ✅ pydantic Optional 回落 + 前端旧卡提示文案 |
| `_product_type_options` 手写 `/` 切分 | ❌ **违反 CLAUDE.md"不要重新手写"约定**，已改用 `vocab_utils.extract_chinese_part/extract_english_part` |
| `routers/vocabularies.py` 白名单 | ❌ 漏了 `product_type`（设置侧看不到新词表），已补 |
| `ProductSelect` 兜底 | ✅ 过滤 Unknown 后为空回落内置选项 |
| 诊断盲区 | ❌ 裸 `raise_for_status()` 丢上游响应体，新增 `ai_client._raise_with_body`（截 300 字带进异常消息，异常类型不变、重试判断不受影响） |
| 前端 lint | 42 problems 与基线逐字相同 |

**遗留验证**：上游恢复后需重跑一次带 hint 的分析确认全链路（词表 55 项 +
`Lantern 灯笼` + `portrait`）。修复后的失败路径已验证：curl 复现的 502 响应里
`_parse_error` 字段有值，前端新检查能取到并显示原因。

---

##### §6.17 变更记录（十）· 2026-08-18（收尾验证中发现：第3层维度名语言未钉死）

**发现过程**：收尾端到端验证时，同一张灯笼图（temperature=0）VLM 把
`replaceable_elements` 的 key 输出成了**英文 snake_case**（`pet_breed_or_portrait_subject`），
而此前所有规则卡都是中文名。后果有二：① 英文键直接显示在版本B/C 界面上；
② 中文名判重（防线①-d）跨语言失效，元素清单漏进 2 个重复维度。
根因：prompt 的 JSON 示例只写了 `"元素名"` 占位，从未约束 key 的语言；
上游代理不稳定（今日 502 横跳），疑似路由到了不同后端模型，把这个未钉死的
自由度暴露了出来。

**修复**：`prompts/rule_extraction.py` 两处——第3层字段说明明确
「key 必须是简短的中文维度名（如"宠物品种"、"边框花卉风格"），不要用英文、
拼音或 snake_case」；JSON 示例占位符改为 `"[简短中文维度名]"`。

**验证（真实 VLM 输出）**：L3 keys 全中文（宠物品种/姓名文字/纪念年份/悼念文案/
花卉边框风格/底部符号）；元素清单无跨语言重复（宠物品种5候选/花卉边框风格5候选/
底部符号5候选/背景底色1候选）；`Lantern 灯笼` + `portrait` + 画布 1333×2000 同时成立。

**收尾验证期间的上游状况备案**：上游代理当日持续抖动——同一请求几分钟内
200↔502 反复，一次服务端分析返回不可解析内容（直连重放同一请求完全正常）。
所有失败均有 `_parse_error` 原因可见（§6.16 的修复生效）。

#### 三问题修复 + 收尾工单（2026-08-18 追加）

- [x] 2026-08-18 问题1（主体元素缺失）：根因纠正——上游自抠有碎洞且本地转换从未执行（§6.11）；改 `force_opaque=True` 要整图 + 本地 v2"严判背景"抠图（§6.12）。验证：GEN-0175 斑块 0.30%→0.000%、圆环中心透明、24 张历史图零回归
- [x] 2026-08-18 问题2（元素比例基于图案而非文件）：`artwork_orientation` 三档粗分类 + `_apply_orientation` 重排长短边 + 两模板锚定"印刷图案区域"（§6.13）。验证：灯笼图 portrait、画布 2000×2000→1333×2000、尺寸桶换到 1024x1536
- [x] 2026-08-18 问题3（品类识别不出）：受控产品词表注入（格式转 `English 中文`）+ hint 品类硬采纳 + 禁默认毛毯 + Unknown 兜底出口 + 前端过滤（§6.14）。验证：hint 灯笼→`Lantern 灯笼`、反向对照毛毯→`Blanket 毛毯`、注入攻击全挡、RULE-0015 误判纠正
- [x] 2026-08-18 元素变体与可变维度不一致：`_is_abstract_dimension` 实体词优先 + 判重三级化（§6.15）。验证：RULE-0067 边框花卉风格 5 候选恢复、19 组判重用例全对、全库 51 卡维度 213→198 无实体丢失
- [x] 2026-08-18 分析页"解析失败"事件：定位为上游 502 抖动；修复前后端 `_parse_error` 字段名不同步（失败曾被渲染成空壳成功）+ `_raise_with_body` 诊断增强 + `_product_type_options` 改用 vocab_utils + vocabularies 路由白名单补 product_type（§6.16）
- [x] 2026-08-18 第3层维度名钉死中文（§6.17，收尾验证中发现并修复）
- [x] 2026-08-18 **三层对账完成**：CLAUDE.md（新增/改写 9 条约定）、方案文档（§6.11~6.17 变更记录）、本工单（本块）。过期引用扫描干净（FLOOD_WHITE_DIST 仅存于标注"已推翻"的历史记录）；lint 42=42 基线一致；后端全部编译通过
- [ ] 用户验收：① 界面重新分析灯笼图（带 hint），确认品类=Lantern 灯笼、朝向=portrait、维度名全中文；② 元素拆分画布竖版、无重复维度；③ 元素变体图无破洞、环形元素中心透明（注意：上游今日不稳，失败时页面现在会显示具体原因，稍后重试即可）
